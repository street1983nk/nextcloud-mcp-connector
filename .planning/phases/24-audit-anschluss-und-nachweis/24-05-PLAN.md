---
phase: 24-audit-anschluss-und-nachweis
plan: 05
type: tdd
wave: 2
depends_on: ["24-02"]
files_modified:
  - src/mcp_connector/oauth/exchange_dryrun.py
  - tests/unit/test_oauth_exchange_dryrun.py
autonomous: true
requirements: [EXCH-06]

must_haves:
  truths:
    - "Ein vorgelegtes Token wird gegen die aktive Konfiguration geprüft und jeder Prüfschritt bekommt ein benanntes Ergebnis"
    - "Der Trockenlauf löst keinen Nextcloud-Aufruf aus und hinterlässt keine Sitzung und keine Audit-Zeile"
    - "Der nicht ausgeführte Kontoschritt steht benannt in der Antwort und wird nicht weggelassen"
    - "Der Trockenlauf benutzt einen eigenen Schlüsselsatz und verbraucht die Bremsen des laufenden Betriebs nicht"
  artifacts:
    - path: "src/mcp_connector/oauth/exchange_dryrun.py"
      provides: "die Regel des Trockenlaufs als reine Funktion, ohne Route und ohne Umgebung"
      contains: "def "
    - path: "tests/unit/test_oauth_exchange_dryrun.py"
      provides: "je ein gemessener Fall pro Prüfschritt plus das Drift-Gate gegen claims_of"
      contains: "claims_of"
  key_links:
    - from: "src/mcp_connector/oauth/exchange_dryrun.py"
      to: "src/mcp_connector/oauth/jwks.py"
      via: "eine eigene KeySet-Instanz je Trockenlauf"
      pattern: "KeySet"
    - from: "tests/unit/test_oauth_exchange_dryrun.py"
      to: "src/mcp_connector/oauth/exchange.py"
      via: "AST-Gate: jede Prüfung von claims_of hat einen Schritt im Trockenlauf"
      pattern: "_refused"
---

<objective>
Die Regelhälfte des Trockenlauf-Kommandos: ein Modul mit reinen Funktionen, das ein vorgelegtes Token gegen eine übergebene Exchange-Konfiguration prüft und eine Liste benannter Schritte mit je einem benannten Ergebnis zurückgibt. Keine Route, keine Umgebung, kein Nextcloud-Aufruf, keine Sitzung, keine Audit-Zeile.

Purpose: Erfolgskriterium 3 verlangt ein benanntes Ergebnis je Prüfschritt und verbietet dem Token ausdrücklich jede Wirkung. Die Regel gehört zum Prüfer und die Konsole zur Shell (24-RESEARCH.md, D3), genau wie `audit/store.verify_chains` die Regel hält und `exapp/audit_verify.py` die Konsole.

Entschieden in diesem Plan (Open Question 2 der Recherche): die Regel wird so gebaut, dass eine zweite Oberfläche später nur noch eine Datei ist. In dieser Phase bekommt nur der ExApp-Betrieb eine Oberfläche (occ, Plan 24-06); der Standalone-Betrieb bekommt keine, und die Grenze wird in `docs/token-exchange.md` (Plan 24-09) ausdrücklich benannt statt stillschweigend gelassen.

Output: `oauth/exchange_dryrun.py` plus ein Drift-Gate, das strukturell ausschließt, dass der Trockenlauf grün sagt, wo der Betrieb rot sagt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-audit-anschluss-und-nachweis/24-RESEARCH.md
@.planning/phases/24-audit-anschluss-und-nachweis/24-PATTERNS.md
@.planning/phases/24-audit-anschluss-und-nachweis/24-02-SUMMARY.md

<interfaces>
Die Verträge, gegen die dieser Plan baut (gelesen am 2026-09-23):

`src/mcp_connector/oauth/exchange.py:325-480` -- `ExchangeTokenChecker.__init__` baut seinen
`KeySet` mit `origin=settings.jwks_origin or settings.issuer`, `jwks_uri`,
`algorithms=settings.algorithms`, `refuse=_refused`, `clock=self._clock`. `claims_of(token)`
prüft in dieser Reihenfolge: Token leer, Größe gegen `MAX_TOKEN_BYTES`, Kodierbarkeit,
Header lesbar, `alg` in der Allowlist, `kid` vorhanden, Header-`typ` verträglich, Payload
lesbar, `iss`-Vorfilter auf dem ungeprüften Payload, Schlüsselabruf, Standard-Claims über
PyJWT, Audience exakt, `azp` als Zeichenkette, `azp` in der Allowlist, `typ` ist Bearer und
nicht ID, `iat`/`exp` numerisch, maximale Lebensdauer, maximales Alter, `sub` brauchbar.
Der Ordnungskommentar (Zeilen 349-352) sagt, warum die Reihenfolge so ist: die billigen
lokalen Regeln fallen zuerst, der ausgehende Schlüsselabruf passiert nur für ein Token, das
schon nach unserem Issuer aussieht, und die signaturgedeckten Prüfungen sind das letzte Wort
über alles, was die früheren Schritte ungeprüft gelesen haben.

`src/mcp_connector/oauth/exchange.py:95-104` -- `MAX_TOKEN_BYTES = 8192`, geprüft **vor** dem
ersten base64-Schritt.

`src/mcp_connector/oauth/jwks.py:70-140` -- `JWKS_CACHE_SECONDS = 300`,
`JWKS_KID_COOLDOWN_SECONDS = 60`, `JWKS_FAILURE_RETRY_SECONDS = 10`, `MAX_KEYS = 20`,
`MAX_RESPONSE_BYTES`; `KeySet(origin=..., jwks_uri=..., algorithms=..., refuse=..., clock=...)`.

`src/mcp_connector/oauth/mapping.py` -- `principal_from_claims(claims, profile)` liefert den
kanonischen Principal oder `None`; die beiden Profile sind `MAPPING_ACCOUNT_ID_V1` und
`MAPPING_USER_OIDC_SUB_V1`.

`src/mcp_connector/oauth/chain.py:19-65` und `load_exchange_config(env)` -- die geladene
`ExchangeConfig` mit `settings` und `mapping`. Der Trockenlauf bekommt sie hereingereicht und
liest die Umgebung nie selbst.

`src/mcp_connector/audit/store.py:436-456` -- `ChainFinding` ist das Vorbild für ein Ergebnis,
das kein fertiger Satz ist: die Regel antwortet in Daten, die Shell macht daraus Text und JSON.

`src/mcp_connector/exapp/audit_verify.py:124-129` -- `LIMIT_SENTENCE`, das Vorbild für den
Ehrlichkeitssatz am Ende jeder Antwort: er gehört in die Antwort und nicht nur in einen
Docstring, weil ein grünes Ergebnis von dem beurteilt wird, der die Konsole liest.

`src/mcp_connector/oauth/exchange_accounts.py:42-66` -- `ExchangeAccounts.identity_for` ist der
Schritt, den der Trockenlauf NICHT ausführen darf, weil er ein Nextcloud-Aufruf ist.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Das Ergebnismodell und die lokalen Prüfschritte</name>
  <files>src/mcp_connector/oauth/exchange_dryrun.py, tests/unit/test_oauth_exchange_dryrun.py</files>
  <read_first>
    - src/mcp_connector/oauth/exchange.py (Zeilen 90-170 die Grenzen und `_refused`, 320-480 `claims_of` Schritt für Schritt)
    - src/mcp_connector/oauth/exchange_accounts.py (Modulform: was ein Modul ohne Umgebung und ohne Netz aussieht)
    - src/mcp_connector/audit/store.py (Zeilen 430-460: `ChainFinding` als Vorbild für ein Ergebnis als Daten)
    - src/mcp_connector/errors.py (die sechs Exchange-Bezeichner aus Plan 24-02)
    - src/mcp_connector/oauth/chain.py (Zeilen 1-70: die `ExchangeConfig` und ihre Felder)
  </read_first>
  <behavior>
    - Ein Ergebnis ist ein frozen dataclass je Schritt mit mindestens: einem stabilen Schrittbezeichner (maschinenlesbar, aus einer geschlossenen Menge), dem Ergebnis (`passed`, `failed`, `skipped`, `not_checked`) und dem Bezeichner der Ablehnungsgruppe, falls der Schritt gefallen ist.
    - Ein Schrittergebnis trägt nie einen Wert aus dem Token: kein `azp`, kein `iss`, kein `aud`, kein `sub`, keine `kid`, kein Tokenteil. Der Schritt sagt, welche Regel fiel, nicht welchen Wert er gesehen hat.
    - Die Liste bricht nach dem ersten gefallenen Schritt nicht ab: jeder nachfolgende Schritt steht mit `skipped` in der Antwort, damit ein Admin die Reihenfolge und die Vollständigkeit sieht.
    - Ein leeres Token fällt im ersten Schritt; ein Token von 8193 Byte fällt im Größenschritt, und zwar vor jedem base64-Schritt; ein Token ohne zwei Punkte fällt im Formschritt.
    - Alles in diesem Task läuft ohne Netz: kein `httpx`-Aufruf, kein `respx`-Mock nötig.
  </behavior>
  <action>
Lege `src/mcp_connector/oauth/exchange_dryrun.py` an. Der Moduldocstring übernimmt die Anspruchsformel von `oauth/exchange_accounts.py` ("Nothing here talks to anything" wird hier zu: nichts hier liest die Umgebung, und das einzige, was hinausgeht, ist der Schlüsselsatz-Abruf des Tasks 2) und nennt EXCH-06 als das Requirement.

Definiere eine geschlossene Menge von Schrittbezeichnern, einen je Prüfung von `claims_of`, plus den nicht ausgeführten Kontoschritt. Die Bezeichner sind maschinenlesbare Kleinbuchstaben-Namen (`token_present`, `token_size`, `token_shape`, `header_readable`, `algorithm_allowed`, `key_named`, `header_type`, `payload_readable`, `issuer_matches`, `key_available`, `signature_and_standard_claims`, `audience_exact`, `acting_party_named`, `acting_party_allowed`, `token_type_bearer`, `times_numeric`, `lifetime_within_bound`, `age_within_bound`, `subject_usable`, `mapping_yields_principal`, `account_exists`), und die Schrittnamen der Konsole werden erst in Plan 24-06 daraus gemacht. Wenn beim Lesen von `claims_of` ein Schritt auftaucht, der in dieser Liste fehlt, kommt er dazu: die Liste folgt dem Prüfer und nicht umgekehrt.

Definiere die vier Ergebniswerte als Konstanten mit einem Satz je Wert: `passed`, `failed`, `skipped` (der Schritt kam nach dem gefallenen), `not_checked` (der Schritt wurde bewusst nicht ausgeführt, und der Grund steht daneben).

Baue das frozen dataclass `DryRunStep` mit `step`, `outcome` und `reason: str | None` (einer der Bezeichner aus `errors.REASONS`), und ein zweites `DryRunResult` mit der Schrittliste, einem Gesamtschlüssel `passed: bool` und dem Ehrlichkeitssatz. Der Docstring von `DryRunStep` übernimmt die Begründung von `ChainFinding`: das ist gemessen und kein fertiger Satz; die Formulierung, die ein Administrator liest, baut die Shell.

Baue die lokalen Schritte bis einschließlich des `iss`-Vorfilters, in genau der Reihenfolge von `claims_of` und mit genau denselben Grenzen (`MAX_TOKEN_BYTES` wird importiert und nie erneut getippt). Der Docstring der Hauptfunktion übernimmt den Ordnungskommentar von `claims_of` wörtlich als Begründung, warum der Trockenlauf die Reihenfolge nicht bequemer machen darf.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_oauth_exchange_dryrun.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Ein Test je lokalem Schritt weist nach, dass genau dieser Schritt `failed` trägt und jeder folgende `skipped`.
    - Ein Test sichert zu, dass kein Feld eines `DryRunStep` jemals einen Wert aus dem vorgelegten Token trägt: er baut ein Token mit einem auffälligen Markertext in `azp`, `iss`, `aud`, `sub` und `kid` und prüft, dass der Marker in der serialisierten Antwort nicht vorkommt.
    - `grep -c "MAX_TOKEN_BYTES" src/mcp_connector/oauth/exchange_dryrun.py` ergibt mindestens 1, und `grep -c "8192" src/mcp_connector/oauth/exchange_dryrun.py` ergibt 0.
    - Kein Test dieses Tasks braucht `respx`.
  </acceptance_criteria>
  <done>Die lokalen Schritte sind gemessen, und kein Tokenwert verlässt die Regel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Der eigene Schlüsselsatz, die signaturgedeckten Schritte und der nicht ausgeführte Kontoschritt</name>
  <files>src/mcp_connector/oauth/exchange_dryrun.py, tests/unit/test_oauth_exchange_dryrun.py</files>
  <read_first>
    - src/mcp_connector/oauth/jwks.py (Zeilen 60-140 die Konstanten und die Konstruktion von `KeySet`, 150-270 `key` und die Bremsen)
    - src/mcp_connector/oauth/exchange.py (Zeilen 325-340 die Konstruktion des KeySet im laufenden Prüfer, 400-480 die signaturgedeckten Schritte)
    - src/mcp_connector/oauth/mapping.py (`principal_from_claims` und die beiden Profile)
    - src/mcp_connector/exapp/audit_verify.py (Zeilen 124-129: `LIMIT_SENTENCE` als Vorbild für den Ehrlichkeitssatz)
    - .planning/phases/24-audit-anschluss-und-nachweis/24-RESEARCH.md (Pitfall 7 und Pitfall 8)
  </read_first>
  <behavior>
    - Der Trockenlauf baut je Lauf eine eigene `KeySet`-Instanz; ein Lauf mit unbekanntem `kid` setzt die Abkühlzeit des laufenden Prüfers nicht. Ein Test misst das: zwei Objekte, zwei unabhängige Zähler, und der laufende Prüfer kann nach dem Trockenlauf einen Abruf auslösen.
    - Ein Lauf kostet genau einen ausgehenden JWKS-Abruf, sobald der `iss`-Vorfilter bestanden ist, und null Abrufe, wenn er vorher fällt (gemessen mit `respx` und `call_count`).
    - Ein Token mit gültiger Signatur, richtiger Audience, gelistetem `azp` und brauchbarem `sub` erreicht `mapping_yields_principal` mit `passed`.
    - Der Schritt `account_exists` steht IMMER in der Antwort und trägt IMMER `not_checked`, mit dem Grund, dass er ein Nextcloud-Aufruf wäre. Er fällt nie weg, auch nicht, wenn ein früherer Schritt gefallen ist (dort steht er als `skipped` mit demselben Hinweis).
    - Ein vollständig grüner Lauf endet mit `passed = true` UND trägt den Ehrlichkeitssatz, der sagt, was ein grüner Lauf nicht bedeutet: die Kontoexistenz wurde nicht geprüft, und auf einer Instanz mit unvollständig antwortender Kontoliste würde derselbe Token im Betrieb abgewiesen.
    - Der Trockenlauf ruft `resolve_identity` nicht auf, legt keine Autorisierung an, öffnet den OAuth-Store nicht und schreibt keine Audit-Zeile. Ein Test weist das an einem Trockenlauf gegen einen Store nach, der bei jedem Zugriff wirft.
  </behavior>
  <action>
Baue in derselben Datei den Schritt `key_available` mit einer eigenen `KeySet`-Instanz, konstruiert nach genau dem Muster aus `ExchangeTokenChecker.__init__` (dieselben fünf Argumente, dieselbe Quelle für `origin` und `jwks_uri`). Der Docstring sagt den Preis und den Grund: ein ausgehender Abruf je Trockenlauf, weil ein Admin-Trockenlauf mit unbekanntem `kid` sonst die 60-Sekunden-Abkühlzeit oder die 10-Sekunden-Fehlerkarenz des heißen Pfads verbrauchen und "Provider nicht erreichbar" melden würde, obwohl nur die Karenz lief. Der Satz über den Preis gehört außerdem in die Antwort, nicht nur in den Docstring.

Baue die signaturgedeckten Schritte in der Reihenfolge von `claims_of` und mit derselben PyJWT-Nutzung. Schreibe die Prüfungen nicht neu, wo eine Funktion schon existiert: `audience_holds` und `principal_from_claims` werden importiert und aufgerufen.

Baue den Schritt `account_exists` als den einen Schritt, der nie ausgeführt wird. Er trägt `not_checked` und einen Grund, der ausspricht, warum: die Kontoexistenz läuft über `ExchangeAccounts.identity_for` und wäre ein Nextcloud-Aufruf, den Erfolgskriterium 3 ausschließt. Ein weggelassener Schritt liest sich als bestandener Schritt (Pitfall 8), also darf er nie fehlen.

Setze den Ehrlichkeitssatz als Modulkonstante nach dem Muster von `LIMIT_SENTENCE`, mit dem `#:`-Kommentar, der sagt, warum er in der Antwort steht und nicht nur im Docstring. Er nennt drei Dinge: die Kontoexistenz wurde nicht geprüft, die Uhr des Trockenlaufs ist die dieses Containers, und ein Token, das hier grün ist, kann im Betrieb trotzdem abgewiesen werden, wenn die Kontoliste der Instanz unvollständig antwortet (Annahme A1 aus `audit/accounts.py`).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_oauth_exchange_dryrun.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Ein `respx`-Test misst `keys.call_count == 0` für ein Token, das am `iss`-Vorfilter fällt, und `== 1` für ein Token, das ihn besteht.
    - Ein Test baut einen laufenden `ExchangeTokenChecker` und einen Trockenlauf gegen denselben Issuer, lässt den Trockenlauf mit unbekanntem `kid` laufen und beweist danach, dass der laufende Prüfer weiterhin einen Abruf auslöst: der Trockenlauf hat die Abkühlzeit des Betriebs nicht verbraucht.
    - Ein Test weist nach, dass `account_exists` in allen drei Fällen (grüner Lauf, früher Fehlschlag, später Fehlschlag) in der Antwort steht.
    - Ein Test lässt einen vollständigen Trockenlauf gegen einen OAuth-Store laufen, dessen Methoden werfen, und beweist, dass der Lauf durchläuft: der Store wird nie angefasst.
    - `grep -vn '^\s*#' src/mcp_connector/oauth/exchange_dryrun.py | grep -c "resolve_identity"` ergibt 0.
  </acceptance_criteria>
  <done>Der Trockenlauf misst alles, was ohne Nextcloud messbar ist, kostet den Betrieb nichts und lügt nicht über das, was er nicht geprüft hat.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Das Drift-Gate gegen den laufenden Prüfer</name>
  <files>tests/unit/test_oauth_exchange_dryrun.py</files>
  <read_first>
    - src/mcp_connector/oauth/exchange.py (alle `_refused(`-Aufrufstellen mit ihrem Bezeichner aus Plan 24-02)
    - src/mcp_connector/oauth/exchange_dryrun.py (die geschlossene Menge der Schrittbezeichner aus Task 1)
    - tests/contract/test_no_destructive_calls.py (Zeilen 295-336: die AST-Maschinerie)
    - tests/unit/test_oauth_exchange.py (das `_refused`-Gate aus Plan 24-02, das dieselbe Maschinerie nutzt)
  </read_first>
  <behavior>
    - Das Gate sammelt über den AST die festen Phrasen aller `_refused(`-Aufrufstellen in `oauth/exchange.py` und hält sie gegen die Prüfschritte in `oauth/exchange_dryrun.py`; es fällt, wenn eine Prüfung des Betriebs keinen Schritt im Trockenlauf hat. Die Prüfung läuft über die **Menge der Phrasen**, nicht über die Zahl der Aufrufstellen: gemessen am 2026-09-23 sind das 19 Aufrufstellen mit 18 unterschiedlichen Phrasen, weil "the token is longer than allowed" an zwei Stellen steht (Zeilen 363 und 369). Ein Gate, das 19 Schlüssel erwartet, wäre schon beim ersten Lauf rot.
    - Das Gate fällt ebenso, wenn ein Schritt des Trockenlaufs eine Ablehnungsgruppe nennt, die die zugehörige `_refused`-Stelle nicht nennt: die Gruppierung der beiden Seiten muss übereinstimmen.
    - Eine Gegenprobe beweist, dass das Gate anschlägt, wenn ein Schritt entfernt wird.
  </behavior>
  <action>
Baue das Gate in `tests/unit/test_oauth_exchange_dryrun.py` nach derselben AST-Maschinerie wie das `_refused`-Gate aus Plan 24-02. Die Abbildung zwischen einer `_refused`-Aufrufstelle und einem Schrittbezeichner muss explizit sein: eine Konstante im Testmodul, die je feste Phrase den Schrittbezeichner nennt, plus eine Zusicherung, dass die Abbildung vollständig ist (jede gefundene Phrase steht in der Konstante) und dass sie in beide Richtungen aufgeht (jeder Schrittbezeichner außer `account_exists` kommt in der Konstante vor).

Die Konstante ist ein `dict[str, str]` von der Phrase auf den Schrittbezeichner, also 18 Einträge für 19 Aufrufstellen: die doppelte Phrase "the token is longer than allowed" (Zeilen 363 und 369) zeigt auf denselben Schritt, und genau darum wird über die Menge der Phrasen verglichen und nie über die Zahl der Aufrufstellen. Ein Kommentar an der Konstante hält das fest, damit die nächste Änderung die Abweichung zwischen 18 und 19 nicht für einen Fehler hält.

Der Testdocstring sagt, was dieses Gate verhindert und was nicht: es verhindert, dass der Betrieb eine Regel bekommt, die der Trockenlauf nicht kennt. Es verhindert nicht, dass beide Seiten dieselbe Regel verschieden umsetzen; dafür sind die gemessenen Fälle aus Task 1 und Task 2 da. Diese Ehrlichkeit gehört in den Docstring, weil ein Gate, von dem man mehr erwartet als es leistet, schlimmer ist als keins.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_oauth_exchange_dryrun.py tests/unit/test_oauth_exchange.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Das Gate läuft grün, und eine Gegenprobe über einen Quelltext-Auszug beweist, dass es bei einem fehlenden Schritt rot wird.
    - Die Abbildungskonstante hat 18 Einträge und nennt jede der 18 unterschiedlichen festen Phrasen aus `oauth/exchange.py`. Nachgemessen: `python -c "import ast; t=ast.parse(open('src/mcp_connector/oauth/exchange.py',encoding='utf-8').read()); a=[c.args[0].value for c in ast.walk(t) if isinstance(c,ast.Call) and getattr(c.func,'id','')=='_refused']; print(len(a), len(set(a)))"` gibt `19 18` aus.
    - Die bidirektionale Abdeckung wird gegen die Menge der Phrasen geprüft, nicht gegen die Zahl der Aufrufstellen: die Menge der im AST gefundenen Phrasen ist gleich der Menge der Schlüssel der Abbildungskonstante, und jeder Schrittbezeichner außer `account_exists` kommt als Wert vor.
    - Der Testdocstring nennt ausdrücklich, was das Gate nicht leistet.
  </acceptance_criteria>
  <done>Eine neue Prüfung im Betrieb kann nicht mehr lautlos am Trockenlauf vorbeigehen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vorgelegtes Token -> Trockenlauf | der Wert kommt von einem Administrator, ist aber inhaltlich fremder Text und wird nie zurückgegeben |
| Trockenlauf -> Identitätsanbieter | ein ausgehender JWKS-Abruf je Lauf |
| Trockenlauf -> laufender Betrieb | der Lauf darf die Bremsen des heißen Pfads nicht verbrauchen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09 | Information Disclosure | `DryRunStep`, `DryRunResult` | mitigate | Kein Feld trägt einen Wert aus dem Token; Markertest über `azp`, `iss`, `aud`, `sub`, `kid` (Task 1) |
| T-24-07 | Denial of Service (Selbstschaden) | `oauth/jwks.KeySet` des laufenden Prüfers | mitigate | Eigene `KeySet`-Instanz je Trockenlauf, gemessen an zwei unabhängigen Zählern (Task 2) |
| T-24-22 | Elevation of Privilege | OAuth-Store, Autorisierungen, Audit-Kette | mitigate | Der Trockenlauf ruft `resolve_identity` nicht auf; Test gegen einen Store, dessen Methoden werfen (Task 2) |
| T-24-23 | Information Disclosure | ein grüner Trockenlauf als falsche Zusicherung | mitigate | `account_exists` steht immer als `not_checked` in der Antwort, plus der Ehrlichkeitssatz nach dem Muster von `LIMIT_SENTENCE` (Task 2) |
| T-24-24 | Tampering | Auseinanderlaufen von Trockenlauf und Betrieb | mitigate | AST-Drift-Gate über die `_refused`-Stellen von `oauth/exchange.py` (Task 3) |
| T-24-25 | Denial of Service | ein sehr großes Token im Trockenlauf | mitigate | Der Größenschritt läuft gegen `MAX_TOKEN_BYTES` vor jedem base64-Schritt, importiert und nie erneut getippt (Task 1) |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | Diese Phase installiert kein Paket (24-RESEARCH.md, "Package Legitimacy Audit") |
</threat_model>

<verification>
- `uv run pytest -q` läuft vollständig grün.
- Qualitätsgates lokal grün VOR dem Commit: `uv run ruff check .`, `uv run ruff format --check .`, `PYRIGHT_PYTHON_FORCE_VERSION=latest uv run pyright`, `uv run vulture src scripts vulture_whitelist.py`. Das Modul hat bis Plan 24-06 keinen Aufrufer in `src/`; die Einträge in `vulture_whitelist.py` bekommen je eine Zeile Begründung und werden in Plan 24-06 wieder entfernt.
- `uv run pytest tests/contract -q` läuft grün.
</verification>

<success_criteria>
- `oauth/exchange_dryrun.py` prüft ein Token gegen eine hereingereichte Konfiguration und liefert je Prüfschritt ein benanntes Ergebnis.
- Kein Wert aus dem vorgelegten Token steht in der Antwort.
- Der Lauf öffnet keinen Store, legt keine Autorisierung an und schreibt keine Audit-Zeile.
- Der Lauf benutzt einen eigenen Schlüsselsatz, gemessen an zwei unabhängigen Zählern.
- Der nicht ausgeführte Kontoschritt steht immer benannt in der Antwort.
</success_criteria>

<output>
Create `.planning/phases/24-audit-anschluss-und-nachweis/24-05-SUMMARY.md` when done
</output>
