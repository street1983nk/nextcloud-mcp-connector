---
phase: 24-audit-anschluss-und-nachweis
plan: 06
type: tdd
wave: 4
depends_on: ["24-04", "24-05"]
files_modified:
  - src/mcp_connector/exapp/exchange_check.py
  - src/mcp_connector/exapp/occ.py
  - src/mcp_connector/entry_exapp.py
  - vulture_whitelist.py
  - tests/unit/test_exapp_exchange_check.py
  - tests/unit/test_exapp_lifecycle.py
  - tests/unit/test_exapp_entry.py
autonomous: true
requirements: [EXCH-06]

must_haves:
  truths:
    - "Ein Administrator prüft ein vorgelegtes Token mit einem occ-Kommando gegen die aktive Konfiguration"
    - "Die Antwort benennt jeden Prüfschritt mit seinem Ergebnis, in Text und in JSON, und immer mit Status 200"
    - "Die Optionsbeschreibung sagt die Folge, dass der Tokenwert in der Prozessliste steht"
    - "Ein Token bis zur harten Tokengrenze kommt durch die Bodygrenze des Handlers, gemessen und nicht angenommen"
  artifacts:
    - path: "src/mcp_connector/exapp/exchange_check.py"
      provides: "Pfadkonstante, Optionsnamen, Routenfabrik, Text- und JSON-Antwort"
      contains: "EXCHANGE_CHECK_PATH"
    - path: "src/mcp_connector/exapp/occ.py"
      provides: "der vierte Kommandoeintrag, Handlername aus der Pfadkonstante abgeleitet"
      contains: "EXCHANGE_CHECK_PATH"
  key_links:
    - from: "src/mcp_connector/exapp/occ.py"
      to: "src/mcp_connector/exapp/exchange_check.py"
      via: "execute_handler wird aus EXCHANGE_CHECK_PATH abgeleitet"
      pattern: "removeprefix"
    - from: "src/mcp_connector/entry_exapp.py"
      to: "src/mcp_connector/exapp/exchange_check.py"
      via: "die Route wird in die Routenliste aufgenommen"
      pattern: "exchange_check_routes"
---

<objective>
Die Konsolenhälfte des Trockenlaufs: eine occ-Route nach dem Muster von `exapp/audit_verify.py`, ein vierter Kommandoeintrag in `exapp/occ.py`, und die gemessene Antwort auf die Frage, ob ein mehrere Kilobyte großer Optionswert unverändert durch AppAPI kommt.

Purpose: Erfolgskriterium 3 verlangt ein Kommando. AppAPI liefert die Admin-Grenze bereits, eine deklarierte HTTP-Route würde die Antwort ins Internet stellen (`exapp/audit_verify.py:13-23`), und ein Status außer 200 lässt AppAPI den Körper verwerfen, der die ganze Antwort ist.

Output: Ein lauffähiges `occ mcp_connector:exchange:check`, plus die Messung, die Annahme A5 der Recherche entweder bestätigt oder widerlegt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-audit-anschluss-und-nachweis/24-RESEARCH.md
@.planning/phases/24-audit-anschluss-und-nachweis/24-PATTERNS.md
@.planning/phases/24-audit-anschluss-und-nachweis/24-05-SUMMARY.md
@.planning/phases/24-audit-anschluss-und-nachweis/24-04-SUMMARY.md

<interfaces>
Die Verträge, gegen die dieser Plan baut (gelesen am 2026-09-23):

`src/mcp_connector/exapp/audit_verify.py` -- die Vorlage, Datei für Datei übernehmbar:
Zeilen 13-23 (warum die Route in keinem `<url>` des Manifests steht), 25-33 (warum jede
Antwort 200 ist), 66-77 (`AUDIT_VERIFY_PATH`, `JSON_OPTION`), 97-110 (`MAX_BODY_BYTES = 4096`,
`MAX_ANNOUNCED_DIGITS = 10`, `TRUE_WORDS`), 124-129 (`LIMIT_SENTENCE`), 147-186 (die
Routenfabrik), 253-282 (`_machine_readable` mit dem Schlüssel `broken`), 309-323 (`_guard`),
325-427 (`_wants_json`, `_set_in`, `_is_set`, `_payload`, `_above_the_body_bound`),
430-436 (`_text`).

`src/mcp_connector/exapp/audit_read.py:463-499` -- `_value` und `_given`: der erste Ort, an dem
Optionen mit **Werten** aus dem Körper gelesen werden. Das ist der Analog für den Tokenwert.

`src/mcp_connector/exapp/occ.py:12-18` -- die drei nicht verhandelbaren Regeln: `mode` nur aus
`required`, `optional`, `none` (ein anderer bricht die occ-Kommandozeile der ganzen Instanz,
nicht nur das eigene Kommando); `arguments` bleibt leer; jede Werte-Option trägt `default`.
Zeilen 93/112/132: die Handlernamen werden aus den Pfadkonstanten abgeleitet
(`PFAD.removeprefix("/")`), damit Registrierung und Route nicht auseinanderlaufen können.
Zeilen 174-281: `command_schemes()`, Zeilen 236-259 der Eintrag mit Werte-Optionen,
Zeilen 101-103 `OCC_FORCE_DESCRIPTION` als Vorbild für eine Beschreibung, die eine Folge
ausspricht statt sie zu verschweigen. Zeilen 284-336: `register_occ_commands`.

`src/mcp_connector/exapp/responses.bounded_body` -- der eine Weg, einen Anfragekörper zu
begrenzen; ein chunked Request kündigt gar nichts an (IN-01 der Phase-5-Nachprüfung).

`src/mcp_connector/oauth/exchange_dryrun.py` (aus Plan 24-05) -- `DryRunStep`, `DryRunResult`,
die geschlossene Menge der Schrittbezeichner, der Ehrlichkeitssatz als Modulkonstante.

`src/mcp_connector/oauth/exchange.py:95-104` -- `MAX_TOKEN_BYTES = 8192`.

`src/mcp_connector/entry_exapp.py:319-321` -- die Routenliste, in der `purge_routes`,
`audit_verify_routes` und `audit_read_routes` stehen.

`tests/unit/test_exapp_lifecycle.py` -- der Test, der jedes Schema aus `command_schemes()`
gegen die Positivliste der Modi hält.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Die Route des Trockenlaufs</name>
  <files>src/mcp_connector/exapp/exchange_check.py, tests/unit/test_exapp_exchange_check.py</files>
  <read_first>
    - src/mcp_connector/exapp/audit_verify.py (die ganze Datei: Moduldocstring, Konstanten, Routenfabrik, Guard, Body-Leser, `_text`)
    - src/mcp_connector/exapp/audit_read.py (Zeilen 455-505: `_value` und `_given`, der Leser für Optionen mit Werten)
    - src/mcp_connector/exapp/responses.py (`bounded_body`, `json_response`, `NO_STORE`)
    - src/mcp_connector/oauth/exchange_dryrun.py (aus Plan 24-05: das Ergebnismodell und der Ehrlichkeitssatz)
    - src/mcp_connector/oauth/chain.py (`load_exchange_config(env)`: woher die aktive Konfiguration kommt)
    - .planning/phases/24-audit-anschluss-und-nachweis/24-RESEARCH.md (D3, Pitfall 6, Pitfall 8)
    - .planning/phases/24-audit-anschluss-und-nachweis/24-PATTERNS.md (Decision Point 1: Bodygrenze gegen Tokengröße)
  </read_first>
  <behavior>
    - Eine Anfrage mit dem Header `x-origin-ip` bekommt 404; eine ohne gültigen AppAPI-Nachweis bekommt 401; die beiden sind von außen nicht unterscheidbar. Das ist der Guard von `audit_verify`, wörtlich übernommen.
    - Jede andere Antwort ist 200, auch wenn das Token an Schritt 4 scheitert.
    - Die JSON-Antwort trägt den Schlüssel `passed`, den ein Skript beobachtet, weil der Exit-Code immer 0 ist.
    - Die Textantwort nennt jeden Schritt mit seinem Ergebnis, eine Zeile je Schritt, und endet mit dem Ehrlichkeitssatz.
    - Weder Text- noch JSON-Antwort enthält das vorgelegte Token oder einen Teil davon, auch nicht zur Bestätigung.
    - Ein Token von 8192 Byte kommt vollständig durch die Bodygrenze dieses Handlers; ein größeres wird ohne Parsen am Größenschritt abgewiesen und bekommt trotzdem 200 mit einem benannten Ergebnis.
    - Ein Aufruf ohne Tokenwert bekommt 200 und ein benanntes Ergebnis am ersten Schritt, nicht eine Ausnahme.
    - Ist der Exchange-Pfad auf dieser Instanz nicht konfiguriert, sagt die Antwort das in einem eigenen, benannten Ergebnis, statt lauter gefallene Schritte zu zeigen.
  </behavior>
  <action>
Lege `src/mcp_connector/exapp/exchange_check.py` an, gebaut nach `exapp/audit_verify.py`. Der Moduldocstring übernimmt die beiden Begründungen von dort (keine Deklaration im Manifest, immer 200) mit Verweis statt mit Kopie und nennt EXCH-06.

Konstanten: `EXCHANGE_CHECK_PATH = "/exchange-check"`, `JSON_OPTION = "json"`, `TOKEN_OPTION = "token"`. Der `#:`-Kommentar zu `EXCHANGE_CHECK_PATH` sagt, dass `exapp/occ.py` den `execute_handler` daraus ableitet, damit die beiden nicht auseinanderlaufen können.

Bodygrenze: setze `MAX_BODY_BYTES = 16384` statt der 4096 aus `audit_verify`. Der `#:`-Kommentar trägt die Rechnung: die harte Tokengrenze dieser Anwendung ist `MAX_TOKEN_BYTES = 8192`, der Körper trägt das Token als JSON-Zeichenkette plus die übrigen Optionen, und eine Grenze unter der Tokengrenze würde ein zulässiges Token als Formfehler melden statt als das, was es ist. Die Zahl steht als Vielfaches von `MAX_TOKEN_BYTES` da und nicht als Literal, damit die Beziehung sichtbar bleibt. `MAX_ANNOUNCED_DIGITS` und die drei gemessenen Lehren des Body-Lesers werden unverändert übernommen.

Übernimm `_guard` wörtlich (der Docstring nennt bereits `exapp/purge.py` und `exapp/lifecycle.py` als die Geschwister; die neue Datei kommt dazu). Übernimm `_wants_json`, `_payload`, `_above_the_body_bound` und `_text`. Für den Tokenwert nimm den Leser aus `exapp/audit_read._value`.

Baue `exchange_check_routes(env=None)` als Routenfabrik, die genau eine `Route(EXCHANGE_CHECK_PATH, ..., methods=["POST"])` zurückgibt. Die Konfiguration kommt über `load_exchange_config(env)`; ist sie `None`, antwortet der Handler mit einem eigenen, benannten Ergebnis ("der Exchange-Pfad ist auf dieser Instanz nicht konfiguriert") statt mit einer Liste gefallener Schritte, weil eine Liste gefallener Schritte den Admin an das Token statt an die Konfiguration schicken würde.

Baue die beiden Ausgabeformen: `_report(result)` als Text, eine Zeile je Schritt in der Reihenfolge der Regel, plus der Ehrlichkeitssatz als letzte Zeile; `_machine_readable(result)` als JSON mit `checked`, `passed`, der Schrittliste (je Schritt `step`, `outcome`, `reason`) und dem Ehrlichkeitssatz unter einem eigenen Schlüssel. Der Docstring von `_machine_readable` übernimmt die Begründung des `broken`-Schlüssels aus `audit_verify`: der Exit-Code kann es nicht tragen, also trägt dieser Schlüssel es.

Die menschenlesbaren Schrittnamen werden hier gebaut und nicht in `oauth/exchange_dryrun.py`: die Regel antwortet in Daten, die Shell macht daraus Sätze (Vorbild `ChainFinding` gegen `audit_verify._report`).

Der `except Exception as exc`-Zweig folgt der Vorlage: eine ERROR-Zeile mit `type(exc).__name__`, und dieselbe Form in beiden Ausgabeformen.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_exapp_exchange_check.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Ein Test mit `x-origin-ip` bekommt 404, einer ohne AppAPI-Nachweis 401, und ein Test sichert zu, dass die beiden Antwortkörper nichts über den Grund sagen.
    - Ein Test schickt ein Token von genau 8192 Byte und bekommt 200 mit einem Schrittergebnis, das nicht `token_size` ist: die Bodygrenze war nicht der Grund.
    - Ein Test schickt ein Token von 8193 Byte und bekommt 200 mit `token_size` als gefallenem Schritt.
    - Ein Test sichert zu, dass das vorgelegte Token in keiner der beiden Antwortformen vorkommt (Suche nach einem Markerteilstring des Tokens im Antwortkörper).
    - Ein Test ohne konfigurierten Exchange-Pfad bekommt 200 und das eigene benannte Ergebnis.
    - `grep -c "16384" src/mcp_connector/exapp/exchange_check.py` ergibt 0 (die Grenze steht als Rechnung über `MAX_TOKEN_BYTES`).
  </acceptance_criteria>
  <done>Der Handler antwortet auf jeden Fall mit 200 und einem benannten Ergebnis je Schritt, und ein zulässiges Token passt durch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Das vierte occ-Kommando und die Verdrahtung</name>
  <files>src/mcp_connector/exapp/occ.py, src/mcp_connector/entry_exapp.py, vulture_whitelist.py, tests/unit/test_exapp_lifecycle.py, tests/unit/test_exapp_entry.py</files>
  <read_first>
    - src/mcp_connector/exapp/occ.py (Zeilen 1-30 Moduldocstring mit den drei Regeln, 85-135 die Namen und Handlerableitungen, 174-281 `command_schemes`, 284-336 `register_occ_commands`)
    - src/mcp_connector/exapp/exchange_check.py (aus Task 1: `EXCHANGE_CHECK_PATH`, `TOKEN_OPTION`, `JSON_OPTION`)
    - src/mcp_connector/entry_exapp.py (Zeilen 310-330: die Routenliste)
    - tests/unit/test_exapp_lifecycle.py (der Test, der die Modi jedes Schemas gegen die Positivliste hält)
    - vulture_whitelist.py (die Einträge, die die Pläne 24-03 und 24-05 dort hinterlassen haben)
    - .planning/phases/24-audit-anschluss-und-nachweis/24-RESEARCH.md (Pitfall 6: das Token in der Prozessliste)
  </read_first>
  <behavior>
    - `command_schemes()` liefert vier Einträge; der neue heißt `mcp_connector:exchange:check`.
    - Der neue Eintrag hat `arguments: []`, zwei Optionen (`token` mit `mode: "optional"` und `default: None`, `json` mit `mode: "none"`), und einen `execute_handler`, der aus `EXCHANGE_CHECK_PATH` abgeleitet ist.
    - Der bestehende Test über die Positivliste der Modi deckt den neuen Eintrag ab, ohne dass er umgeschrieben werden muss.
    - Die Beschreibung der Token-Option nennt die Folge ausdrücklich: der Wert steht in der Prozessliste und in der Shell-Historie des Nextcloud-Hosts, also gehört dort ein kurzlebiges Testtoken hin und nie ein produktives.
    - Die Route steht in der Routenliste von `entry_exapp.build_exapp_app`, und ein Test gegen die gebaute Anwendung erreicht sie.
  </behavior>
  <action>
Füge in `exapp/occ.py` die Namen des vierten Kommandos ein, nach dem Muster der drei bestehenden: der Kommandoname `mcp_connector:exchange:check` (zwei Ebenen im Namensraum, wie die Audit-Kommandos), der Handlername als `EXCHANGE_CHECK_PATH.removeprefix("/")` mit dem bestehenden Kommentar zur Ableitung, und die Beschreibungen. Die Beschreibung der Token-Option folgt dem Vorbild `OCC_FORCE_DESCRIPTION`: sie spricht die Folge aus statt sie zu verschweigen. Die Beschreibung des Kommandos sagt, was der Trockenlauf nicht tut (kein Nextcloud-Aufruf, keine Sitzung, keine Zeile in der Audit-Kette).

Füge den Schema-Eintrag in `command_schemes()` ein, mit `arguments: []`, den beiden Optionen und `usages`, die beide Formen zeigen. Die Werte-Option trägt `"default": None`, weil AppAPI `$argument['default']` bedingungslos liest. Dupliziere den Kommentarblock über den Modi nicht; verweise darauf.

Nimm `*exchange_check_routes(env)` in die Routenliste von `entry_exapp.py` auf, in derselben Form wie die drei bestehenden Zeilen.

Räume `vulture_whitelist.py` auf: die Einträge, die die Pläne 24-03 und 24-05 dort hinterlassen haben, weil ihre Symbole noch keinen Aufrufer hatten, werden entfernt, sobald der Aufrufer existiert. Ein Whitelist-Eintrag, der nicht mehr nötig ist, ist eine ausgeschaltete Prüfung.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_exapp_lifecycle.py tests/unit/test_exapp_entry.py tests/unit/test_exapp_exchange_check.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from mcp_connector.exapp.occ import command_schemes; s=command_schemes(); print(len(s), [e['name'] for e in s])"` zeigt vier Einträge einschließlich `mcp_connector:exchange:check`.
    - `python -c "from mcp_connector.exapp import occ, exchange_check; e=[x for x in occ.command_schemes() if x['name']=='mcp_connector:exchange:check'][0]; print(e['execute_handler'] == exchange_check.EXCHANGE_CHECK_PATH.removeprefix('/'), e['arguments'], [o['mode'] for o in e['options']])"` gibt `True`, eine leere Argumentliste und nur Modi aus der Positivliste aus.
    - Ein Test gegen `entry_exapp.build_exapp_app` erreicht die neue Route mit gültigen AppAPI-Headern und bekommt 200.
    - Die Beschreibung der Token-Option enthält die Wörter zur Prozessliste; ein Test hält das gegen die Zeichenkette, damit sie bei einer Umformulierung nicht verloren geht.
    - `uv run vulture src scripts vulture_whitelist.py` läuft ohne Befund, und `git diff vulture_whitelist.py` zeigt Entfernungen der in 24-03 und 24-05 hinzugefügten Zeilen.
  </acceptance_criteria>
  <done>`occ mcp_connector:exchange:check` ist registriert, erreichbar und sagt, was es kostet.</done>
</task>

<task type="auto">
  <name>Task 3: Die Messung A5 gegen die laufende Topologie</name>
  <files>src/mcp_connector/exapp/exchange_check.py</files>
  <read_first>
    - src/mcp_connector/exapp/exchange_check.py (die Bodygrenze und ihre Begründung aus Task 1)
    - .planning/phases/24-audit-anschluss-und-nachweis/24-RESEARCH.md (Annahme A5, Environment Availability: die laufenden Container `nc35-caddy`, `nc35-harp`, `nc35-nc` und die ExApp in `nc_app_mcp_connector`)
    - docs/nc35-evidence.md (die Form, in der eine Messung in diesem Repo festgehalten wird)
    - docs/exapp-install.md (wie die ExApp in der lokalen Topologie registriert wird)
  </read_first>
  <action>
Miss gegen die laufende NC-35-HaRP-Topologie, ob `ExAppOccService::buildCommand` einen Optionswert von mehreren Kilobyte unverändert durchreicht. Vorgehen: die ExApp mit dem neuen Kommando in der laufenden Instanz registrieren (`occ app_api:app:register` beziehungsweise der Weg aus `docs/exapp-install.md`), dann `occ mcp_connector:exchange:check --token=<wert> --json` mit Tokenwerten wachsender Länge aufrufen und die Länge messen, die im Handler ankommt. Die zu messenden Punkte: 1 000, 4 000, 8 000 und 8 192 Byte.

Die Länge, die ankommt, wird nicht aus dem Handler herausgeschrieben (das wäre das Token in der Antwort, Pitfall 6). Nimm stattdessen das benannte Ergebnis des Größenschritts als Signal: ein Wert unter der Grenze, der als `token_size` fällt, beweist eine Kürzung unterwegs; ein Wert von 8 192 Byte, der an einem späteren Schritt fällt, beweist, dass er vollständig ankam.

Schreibe das Ergebnis als gemessene Zahl mit Datum in den `#:`-Kommentar der Bodygrenze von `exchange_check.py`, nach dem Muster von `audit/store.used_bytes` (dort steht eine Messung mit Datum und Zahlen im Docstring). Wenn die Messung Annahme A5 widerlegt, also ein langes Token gekürzt ankommt, ist das kein Fehlschlag dieses Tasks: dann wird die gemessene Obergrenze in die Optionsbeschreibung und in `docs/token-exchange.md` (Plan 24-09) aufgenommen, und das SUMMARY sagt es ausdrücklich, damit Plan 24-09 die richtige Zahl schreibt.

Halte im SUMMARY fest: die Container und ihre Versionen, die vier gemessenen Punkte mit ihrem Ergebnis, und die Folgerung. Keine Zahl ohne Messung; ein geschätzter Satz ist genau das, was IN-05 dieser Phase auf eine eigene Aufgabe gebracht hat.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_exapp_exchange_check.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Der `#:`-Kommentar der Bodygrenze in `exchange_check.py` nennt ein Messdatum und mindestens eine gemessene Byte-Zahl.
    - Das SUMMARY dieses Plans enthält die Rohausgabe der vier Läufe mit den Container-Versionen.
    - Wenn A5 widerlegt wurde: die Optionsbeschreibung in `exapp/occ.py` nennt die gemessene Obergrenze, und das SUMMARY sagt Plan 24-09 ausdrücklich, welche Zahl in die Doku gehört.
    - `uv run pytest -q` läuft weiterhin vollständig grün.
  </acceptance_criteria>
  <done>Annahme A5 ist gemessen statt angenommen, und die Zahl steht am Ort ihrer Wirkung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AppAPI -> occ-Route | die einzige Admin-Grenze dieses Kommandos; ein fehlender Nachweis und ein Proxy-Pfad müssen ununterscheidbar enden |
| Administrator -> Prozessliste des Nextcloud-Hosts | der Tokenwert steht als Optionswert in `ps` und in der Shell-Historie |
| Anfragekörper -> Handler | der Körper ist Eingabe von außen und wird vor dem Parsen begrenzt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-08 | Elevation of Privilege | `exchange_check._guard` | mitigate | Der Guard von `audit_verify` wörtlich übernommen: `x-origin-ip` endet mit 404, fehlender AppAPI-Nachweis mit 401, beide ohne Detail (Task 1) |
| T-24-06 | Information Disclosure | Optionswert in `ps` und Shell-Historie | mitigate | Kein technischer Ausweg (Datei liegt auf dem falschen Dateisystem, stdin wird nicht weitergereicht); die Folge wird in der Optionsbeschreibung benannt, die Antwort wiederholt den Wert nie, und die eigene Lebensdauergrenze von 900 s begrenzt den Schaden (Task 1 und Task 2) |
| T-24-09 | Information Disclosure | Text- und JSON-Antwort | mitigate | Test mit Markerteilstring des Tokens im Antwortkörper (Task 1) |
| T-24-25 | Denial of Service | Anfragekörper des Handlers | mitigate | `bounded_body` mit angekündigter Länge zuerst, `MAX_ANNOUNCED_DIGITS`, Grenze als Rechnung über `MAX_TOKEN_BYTES` (Task 1) |
| T-24-26 | Denial of Service | occ-Kommandozeile der ganzen Instanz | mitigate | Modi nur aus der Positivliste, `arguments` leer, jede Werte-Option mit `default`; der bestehende Test in `test_exapp_lifecycle.py` deckt den neuen Eintrag ab (Task 2) |
| T-24-27 | Information Disclosure | Route im Manifest | mitigate | Die Route steht in keinem `<url>` des Manifests; eine Deklaration würde die Antwort ins Internet stellen (Task 1, Moduldocstring) |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | Diese Phase installiert kein Paket (24-RESEARCH.md, "Package Legitimacy Audit") |
</threat_model>

<verification>
- `uv run pytest -q` läuft vollständig grün.
- Qualitätsgates lokal grün VOR dem Commit: `uv run ruff check .`, `uv run ruff format --check .`, `PYRIGHT_PYTHON_FORCE_VERSION=latest uv run pyright`, `uv run vulture src scripts vulture_whitelist.py` ohne Befund und ohne übrig gebliebene Whitelist-Einträge aus 24-03 und 24-05.
- `uv run pytest tests/contract -q` läuft grün.
- Die Messung aus Task 3 ist im SUMMARY mit Rohausgabe festgehalten.
</verification>

<success_criteria>
- `occ mcp_connector:exchange:check --token=... [--json]` liefert je Prüfschritt ein benanntes Ergebnis, immer mit Status 200.
- Der Aufruf löst keinen Nextcloud-Aufruf aus, hinterlässt keine Sitzung und keine Audit-Zeile.
- Ein Token bis 8192 Byte kommt gemessen durch die Bodygrenze.
- Die Optionsbeschreibung benennt die Folge, dass der Wert in der Prozessliste steht.
</success_criteria>

<output>
Create `.planning/phases/24-audit-anschluss-und-nachweis/24-06-SUMMARY.md` when done
</output>
