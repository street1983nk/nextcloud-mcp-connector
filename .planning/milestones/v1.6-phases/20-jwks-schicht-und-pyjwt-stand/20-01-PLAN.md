---
phase: 20-jwks-schicht-und-pyjwt-stand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - docs/dependency-audit.md
autonomous: true
requirements: [DEP-01]
must_haves:
  truths:
    - "PyJWT wird mit >=2.14,<3 verlangt und der Lock steht auf 2.14.0"
    - "cryptography ist im selben Lock-Schritt mitgezogen und bleibt innerhalb von >=50,<51"
    - "Kein neues Paket ist in den Baum gekommen: die Menge der Paketnamen in uv.lock ist unverändert"
    - "docs/dependency-audit.md nennt die Sicherheitsfreigabe vom 11.09.2026 mit ihren GHSA-Kennungen, sagt welche Befunde den geerbten Code in oauth/oidc.py betreffen und begründet, warum PyJWKClient weiterhin nicht benutzt wird"
    - "Die Versionsstellen des Projekts tragen nach dem Lock-Lauf durchgängig 0.2.1, einschliesslich des Selbsteintrags in uv.lock"
    - "Alle Qualitätsgates sind grün, ohne dass eine Testerwartung gelockert wurde"
  artifacts:
    - path: "pyproject.toml"
      provides: "Die angehobene Untergrenze der einzigen JWT-Bibliothek"
      contains: "pyjwt[crypto]>=2.14,<3"
    - path: "uv.lock"
      provides: "Der aufgelöste Stand: pyjwt 2.14.0, cryptography aus 50.x"
      contains: "version = \"2.14.0\""
    - path: "docs/dependency-audit.md"
      provides: "Nachtrag zur Sicherheitsfreigabe plus die bisher fehlende pyjwt-Zeile der Audit-Tabelle"
      min_lines: 200
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "requires-dist-Eintrag des Projekts"
      pattern: "specifier = \">=2\\.14,<3\""
    - from: "docs/dependency-audit.md"
      to: "uv.lock"
      via: "Nachtrag nennt die aufgelöste Version und den Befehl, der sie erzeugt hat"
      pattern: "2\\.14\\.0"
---

<objective>
PyJWT steigt von `>=2.13,<3` auf `>=2.14,<3` (Lock 2.13.0 auf 2.14.0), cryptography zieht im
selben Lock-Schritt mit, und `docs/dependency-audit.md` bekommt den Nachtrag zur
Sicherheitsfreigabe vom 11.09.2026.

Purpose: Fünf Befunde dieser Freigabe liegen im JWKS-Pfad, drei davon betreffen genau den Code,
den Plan 20-02 herauslöst und danach an einer vor-authentischen Stelle wiederverwendet: der
unbegrenzte Neuabruf bei unbekanntem `kid` (GHSA-2gx3-rcp4-g85q), Umleitungen beim JWKS-Abruf
(GHSA-9v7f-9g4p-ffgj) und unbehandelte Ausnahmen bei kaputten JWK-Einträgen
(GHSA-w6j9-cwv2-h6wq, GHSA-8wjv-2p76-3863), die auf 2.13 aus einer Abweisung eine 500 machen.
Die Anhebung steht deshalb vor der Herauslösung und nicht danach: der Umbau soll auf der
Zielversion stattfinden, und ein `uv sync` mitten in einem laufenden Testlauf des Nachbarplans
wäre eine vermeidbare Störquelle.

Output: angehobene Untergrenze, neu aufgelöster Lock, Audit-Nachtrag, alle Gates grün.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
</context>

<interfaces>
Stand beim Planen (2026-09-19), beim Lesen gegenprüfen:

- `pyproject.toml:18` `"pyjwt[crypto]>=2.13,<3"`, `pyproject.toml:16` `"cryptography>=50,<51"`.
  Der cryptography-Bereich bleibt unverändert, nur der Lock zieht innerhalb davon nach.
- `uv.lock:504` `{ name = "pyjwt", extras = ["crypto"], specifier = ">=2.13,<3" }`,
  `uv.lock:648` `[[package]] name = "pyjwt"` mit `version = "2.13.0"`.
- `uv.lock:471` `[[package]] name = "nextcloud-mcp-connector"` trägt heute `version = "0.2.0"`,
  `pyproject.toml:3` trägt `0.2.1`. Das ist die ungegatete sechste Versionsstelle aus der Lehre
  von Release 0.2.1: ein Lock-Lauf korrigiert sie auf 0.2.1. Diese Korrektur ist erwünscht und
  gehört ins SUMMARY, sie ist kein Kollateralschaden.
- `docs/dependency-audit.md:16-35` ist die Audit-Tabelle. `pyjwt` hat dort **keine** Zeile,
  obwohl es seit Phase 03 direkte Abhängigkeit ist; `cryptography` steht in Zeile 35 mit
  "Approved (direct) 2026-08-16".
- `docs/dependency-audit.md:104-135` ist ein auf den 14.08.2026 datierter `uv tree --depth 2`.
  Dieser Schnappschuss wird **nicht** umgeschrieben, er ist datiert. Der neue Stand kommt als
  eigener, neu datierter Auszug in den Nachtrag.
- `.github/workflows/ci.yml:22-36` sind die Gates: `ruff check .`, `ruff format --check .`,
  `pyright`, `vulture src scripts vulture_whitelist.py`, `pytest tests/unit tests/contract`.
- Die einzigen Stellen im Produktionsbaum, die PyJWT direkt benutzen, sind
  `src/mcp_connector/oauth/oidc.py` (`jwt.get_unverified_header`, `jwt.decode`, `jwt.PyJWK`,
  `jwt.PyJWTError`) und `src/mcp_connector/oauth/crypto.py`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Untergrenze anheben und den Lock gezielt neu auflösen</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (Zeilen 1-30: Version, dependencies, insbesondere Zeile 16 und 18)
    - uv.lock (Zeilen 468-512: Selbsteintrag und package.metadata; Zeilen 640-660: pyjwt-Eintrag)
    - .planning/research/STACK.md (Zeilen 20-30 und 81-96: Begründung der Anhebung, Kompatibilität mit mcp 2.0.0)
    - src/mcp_connector/oauth/oidc.py (Zeilen 271-335 und 388-417: die Aufrufe, die 2.14 betrifft)
    - docs/dependency-audit.md (Zeilen 159-180: Supply-Chain-Regeln, uv sync --frozen)
  </read_first>
  <action>
    Vor der ersten Änderung drei Werte notieren, sie sind später die Gegenprobe:
    `grep -c '^name = ' uv.lock` (Zahl der Pakete im Lock), sowie die sha256-Summen von
    `README.md`, `README.de.md`, `README.fr.md` und `appinfo/info.xml`.

    In `pyproject.toml` genau eine Zeichenkette ändern: `"pyjwt[crypto]>=2.13,<3"` wird
    `"pyjwt[crypto]>=2.14,<3"`. Die cryptography-Zeile bleibt `"cryptography>=50,<51"`; sie ist
    ein Bereich, kein Pin, und 50.0.1 liegt darin.

    Danach genau zwei Befehle, in dieser Reihenfolge:
    `uv lock --upgrade-package pyjwt --upgrade-package cryptography`, dann `uv sync`.
    `uv lock --upgrade` ohne Paketnamen ist verboten: das wäre ein Rundumschlag über alle
    Abhängigkeiten in einem Plan, der zwei Pakete anfassen soll.

    Erwartete Änderungen im Lock, und keine anderen: der `[[package]] name = "pyjwt"`-Block auf
    2.14.0 mit neuen Hashes, der cryptography-Block auf die aktuelle 50.x mit neuen Hashes, der
    `package.metadata`-Eintrag auf `specifier = ">=2.14,<3"`, und der Selbsteintrag
    `nextcloud-mcp-connector` von `0.2.0` auf `0.2.1`. Wenn eine weitere Paketzeile auftaucht
    oder verschwindet, ist das kein erwartetes Ergebnis: dann zurückrollen und im SUMMARY als
    Blocker melden.

    Wenn `uv lock` 2.14.0 nicht auflösen kann (Netz, Yank, andere Auflösung), wird der Plan hier
    gestoppt und als Blocker gemeldet. Kein Ausweichen auf eine andere Version, kein anderes
    Paket, kein `--prerelease`. Die Sicherheitsfreigabe ist der Zweck der Änderung; irgendeine
    andere Version erfüllt DEP-01 nicht.

    Zum Schluss den vollen Gate-Lauf fahren (ruff check, ruff format --check, pyright, vulture,
    pytest tests/unit tests/contract). Wenn ein bestehender Test durch 2.14 rot wird, wird die
    Ursache behoben oder gemeldet, nie die Erwartung gelockert; die wahrscheinlichste Stelle ist
    `_usable_key` in `oauth/oidc.py`, das bei `jwt.PyJWK(entry)` nur `jwt.PyJWTError` fängt.
  </action>
  <verify>
    <automated>uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src scripts vulture_whitelist.py && uv run pytest tests/unit tests/contract -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pyjwt\[crypto\]>=2.14,<3' pyproject.toml` ergibt 1
    - `grep -c 'pyjwt\[crypto\]>=2.13,<3' pyproject.toml` ergibt 0
    - `grep -A1 '^name = "pyjwt"' uv.lock | grep -c 'version = "2.14.0"'` ergibt 1
    - `grep -c 'specifier = ">=2.14,<3"' uv.lock` ergibt 1
    - `grep -A1 '^name = "cryptography"' uv.lock | grep -c 'version = "50\.'` ergibt 1
    - `uv run python -c "import jwt; print(jwt.__version__)"` gibt `2.14.0` aus
    - `grep -c '^name = ' uv.lock` ergibt dieselbe Zahl wie vor der Änderung (kein neues Paket, kein entferntes)
    - `grep -A1 '^name = "nextcloud-mcp-connector"' uv.lock | grep -c 'version = "0.2.1"'` ergibt 1
    - `grep -c '^version = "0.2.1"' pyproject.toml` ergibt 1 und `grep -c '^__version__ = "0.2.1"' src/mcp_connector/__init__.py` ergibt 1
    - `grep -o '<version>[^<]*' appinfo/info.xml` und `grep -o '<image-tag>[^<]*' appinfo/info.xml` nennen je `0.2.1`
    - Die sha256-Summen von `README.md`, `README.de.md`, `README.fr.md` und `appinfo/info.xml` sind unverändert
    - `uv run --no-sync pytest tests/unit/test_exapp_env_setup.py -q` endet mit 0
    - Der volle Gate-Lauf aus `<verify>` ist grün, und keine Datei unter `tests/` wurde angefasst
  </acceptance_criteria>
  <done>PyJWT 2.14.0 ist gefordert, gelockt und installiert, cryptography ist mitgezogen, der Lock hat kein Paket mehr und keins weniger, die Versionsstellen stimmen wieder überein und alle Gates sind grün.</done>
</task>

<task type="auto">
  <name>Task 2: Nachtrag in docs/dependency-audit.md</name>
  <files>docs/dependency-audit.md</files>
  <read_first>
    - docs/dependency-audit.md (ganz; besonders Zeilen 14-38 Audit-Tabelle, 64-103 "Promoting cryptography", 104-135 datierter uv tree, 159-180 Supply-Chain-Regeln und der Abschnitt "Upgrade check" als Formvorbild)
    - .planning/research/STACK.md (Zeilen 81-96: die fünf Befunde mit ihren GHSA-Kennungen und was sie für den geerbten Code bedeuten; Zeilen 144-148: warum PyJWKClient und die Alternativen ausscheiden)
    - uv.lock (das Ergebnis von Task 1: aufgelöste Versionen von pyjwt und cryptography)
  </read_first>
  <action>
    Zwei Ergänzungen, beide in englischer Sprache wie der Rest der Datei, ohne Em-Dashes.

    Erstens eine Zeile für `pyjwt` in der Audit-Tabelle (Zeilen 14-38), im Format der übrigen
    Zeilen: Registry PyPI, Alter mit dem Datum von 2.14.0 (2026-09-11) und dem Projektalter,
    Downloads, Quell-Repo `github.com/jpadilla/pyjwt`, slopcheck-Feld wie bei cryptography
    ("not runnable here, verified by hand", weil dasselbe Verfahren gilt), Disposition
    "Approved (direct)" mit dem Datum dieses Plans und dem Verweis auf den neuen Abschnitt. Die
    Zeile fehlte bisher, obwohl das Paket seit Phase 03 direkte Abhängigkeit ist; das ist die
    eigentliche Lücke, die hier nebenbei geschlossen wird, und der Satz darf so dastehen.

    Zweitens ein neuer Abschnitt am Ende der Datei, Überschrift in der Form der bestehenden
    Abschnitte, etwa `## Raising PyJWT to 2.14 (security release 2026-09-11, plan 20-01)`. Er
    hält fünf Dinge fest:
    1. Was angehoben wurde und womit: die eine Zeile in `pyproject.toml`, der Befehl
       `uv lock --upgrade-package pyjwt --upgrade-package cryptography`, die aufgelösten
       Versionen, und dass kein Paketname hinzugekommen oder weggefallen ist (die Zahl aus der
       Gegenprobe von Task 1 nennen).
    2. Warum: die fünf Befunde mit ihren Kennungen `GHSA-2gx3-rcp4-g85q`,
       `GHSA-9v7f-9g4p-ffgj`, `GHSA-w6j9-cwv2-h6wq`, `GHSA-8wjv-2p76-3863`,
       `GHSA-r6x4-923q-g947`, je ein Satz, und ausdrücklich welche drei den eigenen geerbten
       Code in `oauth/oidc.py` betreffen und welche nicht (die HMAC-Härtung trifft uns nur
       mittelbar, weil `HS*` in der Allowlist gar nicht vorkommt).
    3. Warum `jwt.PyJWKClient` trotz der dortigen Korrekturen weiterhin nicht benutzt wird:
       synchron auf `urllib`, blockiert im ASGI-Server den Event-Loop, und ihm fehlen
       Gleich-Origin-Prüfung gegen den Issuer, Grössenlimit auf der Antwort,
       Schlüsseltyp-Allowlist und die Behandlung eines doppelt vergebenen `kid`. Sein Nutzen ist
       die Vorlage (Abkühlzeit, Serialisierung, Cache nie bei Fehlern leeren), nicht der Code.
    4. Dass cryptography im selben Lock-Schritt mitgezogen wurde und warum das kein
       API-Wechsel ist (Bereich `>=50,<51` unverändert, neu gebautes Wheel).
    5. Ein frisch datierter Auszug aus `uv tree --depth 2`, beschränkt auf die Zeilen mit
       `pyjwt` und `cryptography`, als Beleg. Der auf den 14.08.2026 datierte Schnappschuss
       weiter oben in der Datei bleibt unangetastet: er ist datiert und beschreibt einen
       vergangenen Stand.

    Die Datei darf keine deutschen Absätze bekommen und keine Sicherheitsaussage treffen, die
    nicht belegt ist. Wo eine Angabe nicht selbst geprüft wurde (etwa Downloadzahlen), wird sie
    in derselben Vorsicht formuliert wie die bestehenden Zeilen.
  </action>
  <verify>
    <automated>uv run ruff check docs/dependency-audit.md && uv run ruff format --check docs/dependency-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'GHSA-2gx3-rcp4-g85q\|GHSA-9v7f-9g4p-ffgj\|GHSA-w6j9-cwv2-h6wq\|GHSA-8wjv-2p76-3863\|GHSA-r6x4-923q-g947' docs/dependency-audit.md` ergibt mindestens 5
    - `grep -c '^| pyjwt |' docs/dependency-audit.md` ergibt 1
    - `grep -c '2.14.0' docs/dependency-audit.md` ergibt mindestens 2
    - `grep -c 'PyJWKClient' docs/dependency-audit.md` ergibt mindestens 1
    - `grep -c '2026-08-14' docs/dependency-audit.md` ergibt weiterhin 1 (der datierte Schnappschuss ist nicht umgeschrieben worden)
    - `grep -c 'uv lock --upgrade-package pyjwt' docs/dependency-audit.md` ergibt mindestens 1
    - Die Datei enthält keinen Em-Dash: `grep -cP '\x{2014}' docs/dependency-audit.md` ergibt 0
    - Ausser `docs/dependency-audit.md` hat dieser Task keine Datei angefasst
  </acceptance_criteria>
  <done>Die Audit-Datei nennt pyjwt in der Tabelle, trägt den datierten Nachtrag mit den fünf Kennungen, der Begründung gegen PyJWKClient und dem frischen Baumauszug, und der alte Schnappschuss steht unverändert daneben.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI zu Bauumgebung | Zwei Paketversionen wechseln; alles, was der Lock aufnimmt, läuft später im Container |
| Lockfile zu CI | CI installiert mit `uv sync --frozen`; was hier gelockt wird, ist das, was ausgeliefert wird |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-20-SC | Tampering (Supply Chain) | medium | `pyproject.toml`, `uv.lock` | mitigate | Kein neues Paket: die Änderung hebt zwei bereits geprüfte direkte Abhängigkeiten an. `uv lock` läuft mit `--upgrade-package` auf genau diese zwei Namen, nie als Rundumschlag. Gegenprobe: `grep -c '^name = ' uv.lock` ist vor und nach der Änderung gleich. `pyjwt` bekommt in derselben Änderung seine bislang fehlende Zeile in der Audit-Tabelle, cryptography steht dort seit 2026-08-16. Keine Substitution bei Fehlschlag: eine nicht auflösbare 2.14.0 stoppt den Plan |
| T-20-02 | Elevation of Privilege | high | JWKS-Pfad in `oauth/oidc.py` unter PyJWT 2.13 | mitigate | Genau die Anhebung dieses Plans: 2.14.0 schliesst drei Befunde, die den geerbten Code unmittelbar treffen (unbegrenzter Neuabruf bei unbekanntem `kid`, Umleitungen beim Abruf, unbehandelte Ausnahmen bei kaputten JWK-Einträgen). Der Rest der Antwort auf den ersten Befund ist eigener Code und steht in Plan 20-02 |
| T-20-03 | Denial of Service | low | Lock-Drift zwischen Entwicklungsbaum und CI | mitigate | `uv sync` nach dem Lock, voller Gate-Lauf im selben Task, und der Selbsteintrag des Locks wird auf die Projektversion 0.2.1 gezogen, statt weiter auf 0.2.0 zu stehen |
</threat_model>

<verification>
1. `uv run ruff check . && uv run ruff format --check .` über das ganze Repo, still.
2. `uv run pyright` ohne neue Meldung.
3. `uv run vulture src scripts vulture_whitelist.py` still.
4. `uv run pytest tests/unit tests/contract -q` grün, ohne eine geänderte Erwartung.
5. `uv run python -c "import jwt; print(jwt.__version__)"` gibt 2.14.0.
6. Unter `src/`, `tests/` und `appinfo/` ist keine Datei geändert worden.
7. Nach jedem `gsd-sdk`-Aufruf `.planning/STATE.md` und `.planning/ROADMAP.md` ansehen
   (Prozentwert, `last_activity`, Tabellentrenner).
</verification>

<success_criteria>
- `pyjwt[crypto]>=2.14,<3` in `pyproject.toml`, `2.14.0` im Lock, cryptography im selben Schritt mitgezogen
- Keine neue und keine verschwundene Paketzeile im Lock
- `docs/dependency-audit.md` trägt die pyjwt-Zeile und den datierten Nachtrag mit fünf GHSA-Kennungen
- Versionsstellen wieder deckungsgleich auf 0.2.1, einschliesslich Selbsteintrag im Lock
- Alle vier Gates grün, keine Testdatei angefasst
</success_criteria>

<output>
Create `.planning/phases/20-jwks-schicht-und-pyjwt-stand/20-01-SUMMARY.md` when done.
Im SUMMARY ausdrücklich festhalten: die aufgelösten Versionen von pyjwt und cryptography, die
Zahl der Paketzeilen vor und nach dem Lock-Lauf, und dass der Selbsteintrag des Locks von 0.2.0
auf 0.2.1 gezogen wurde (die ungegatete sechste Versionsstelle aus der Lehre von Release 0.2.1).
</output>
