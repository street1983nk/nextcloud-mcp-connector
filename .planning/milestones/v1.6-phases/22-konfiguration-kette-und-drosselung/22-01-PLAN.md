---
phase: 22-konfiguration-kette-und-drosselung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_connector/config.py
  - src/mcp_connector/oauth/chain.py
  - src/mcp_connector/entry_exapp.py
  - src/mcp_connector/entry_oauth.py
  - tests/unit/test_config.py
  - tests/unit/test_oauth_exchange_chain.py
  - tests/unit/test_exapp_entry.py
  - tests/unit/test_entry_oauth.py
  - CHANGELOG.md
autonomous: true
requirements: [CONF-01]
must_haves:
  truths:
    - "Im Werkszustand (keine einzige NC_MCP_EXCHANGE_*-Variable gesetzt) verhält sich der Server wie heute: select_mode kennt keinen sechsten Modus und kein Aufruf liest eine Exchange-Konfiguration ein"
    - "Der Exchange-Pfad hat einen eigenen Namensraum NC_MCP_EXCHANGE_* mit einem ausdrücklichen Schalter und steht ab Werk aus"
    - "Die vier von F13 abhängigen Werte (Audience, Konto-Claim, Issuer, azp) sind Konfiguration; Audience und Konto-Claim haben einen dokumentierten Default, Issuer und azp sind bei bewaffnetem Schalter Pflicht"
    - "Eine halb ausgefüllte Konfiguration bricht den Start mit einer benannten Meldung ab, die die fehlende Variable nennt und nie ihren Wert"
    - "Eine gesetzte NC_MCP_EXCHANGE_*-Variable bei ausgeschaltetem Schalter bricht den Start ebenfalls ab, statt still halb zu laufen"
  artifacts:
    - path: "src/mcp_connector/config.py"
      provides: "Die Namen des Exchange-Namensraums und der Schalter selbst, in der Form von audit_log_enabled"
      contains: "ENV_EXCHANGE_ENABLED"
    - path: "src/mcp_connector/oauth/chain.py"
      provides: "load_exchange_config: die Umgebung wird zu einer validierten ExchangeSettings plus Konto-Claim, oder zu None, oder zu einer benannten ToolError"
      exports: ["ExchangeConfig", "load_exchange_config", "DEFAULT_JWKS_PATH"]
      min_lines: 120
    - path: "tests/unit/test_oauth_exchange_chain.py"
      provides: "Der Nachweis des Aus-Zustands, der Defaults und jeder Startabweisung"
      min_lines: 150
  key_links:
    - from: "src/mcp_connector/oauth/chain.py"
      to: "src/mcp_connector/oauth/exchange.py"
      via: "Die gelesene Umgebung wird zu genau der ExchangeSettings, die Phase 21 validiert"
      pattern: "ExchangeSettings\\("
    - from: "src/mcp_connector/oauth/chain.py"
      to: "src/mcp_connector/config.py"
      via: "Jeder Variablenname kommt aus config, nie als Literal im Modul"
      pattern: "config\\.ENV_EXCHANGE_"
    - from: "src/mcp_connector/entry_exapp.py"
      to: "src/mcp_connector/oauth/chain.py"
      via: "build_exapp_app liest die Konfiguration beim Bauen, damit eine halbe Konfiguration den Start abbricht"
      pattern: "load_exchange_config\\("
---

<objective>
Der Exchange-Pfad bekommt seine eigene Konfigurationsfläche `NC_MCP_EXCHANGE_*` mit einem
ausdrücklichen Schalter, dokumentierten Defaults für Audience, Konto-Claim, JWKS-URL und
Algorithmen, und einer Startabweisung in beide Richtungen: bewaffneter Schalter ohne die
Pflichtwerte bricht ab, gesetzte Werte ohne bewaffneten Schalter ebenso.

Purpose: Die Kette in Plan 22-02 muss die Feldnamen der Konfiguration und die Form des
Prüfers bereits kennen, und der Aus-Zustand muss vor der Kette festgenagelt sein, damit
"byte-gleich zu heute" eine Messung ist und keine Behauptung.

Output: Ein Namensraum in `config.py`, ein Leser in `oauth/chain.py`, der eine validierte
`ExchangeSettings` plus Konto-Claim oder `None` oder eine benannte `ToolError` liefert, und
zwei Einstiegspunkte, die eine halbe Konfiguration nicht bedienen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/21-exchange-verifier/21-01-SUMMARY.md
@.planning/phases/21-exchange-verifier/21-02-SUMMARY.md
@src/mcp_connector/config.py
@src/mcp_connector/oauth/exchange.py
@src/mcp_connector/oauth/metadata.py

<interfaces>
Der Vertrag, gegen den dieser Plan baut, wie er wirklich dasteht (im Zweifel in der Datei
nachlesen, nicht raten):

`src/mcp_connector/oauth/exchange.py` (Phase 21, wird in diesem Plan **nicht** angefasst):

- `ExchangeSettings(issuer, jwks_uri, audience, azp_allowed, jwks_origin=None,
  algorithms=DEFAULT_EXCHANGE_ALGORITHMS, leeway_seconds=EXCHANGE_LEEWAY_SECONDS,
  max_lifetime_seconds=MAX_TOKEN_LIFETIME_SECONDS, typ_expected=ACCESS_TOKEN_TYP)`,
  `frozen=True, slots=True`
- `__post_init__` wirft `ValueError` bei: leerem oder nicht-String-Feld, Issuer ohne HTTPS
  oder mit Schrägstrich am Ende, `jwks_uri` ausserhalb der Origin von `jwks_origin or
  issuer`, leerer Audience, `azp_allowed` als blanker String oder leerer Sequenz,
  Algorithmen ausserhalb von `jwks.ALLOWED_ALGORITHMS`, nicht-endlicher oder
  nicht-positiver Sekundenzahl
- `azp_allowed` und `algorithms` sind Sequenzen nicht-leerer Strings; ein blanker String
  ist ausdrücklich verboten (er wäre eine Allowlist seiner Zeichen)
- `ExchangeTokenChecker(settings, *, clock=None, now=None, max_token_bytes=MAX_TOKEN_BYTES)`
  und `await checker.claims_of(token) -> dict[str, Any]` oder `ExchangeRefused`

`src/mcp_connector/config.py` (Muster, wortwörtlich zu übernehmen):

- `audit_log_enabled` ist die Form für einen ab Werk **ausgeschalteten** Schalter: leer,
  unlesbar oder unbekannt bleibt aus, `_TRUE_VALUES`/`_FALSE_VALUES` entscheiden, eine
  Warnung nennt die Variable und **nie** ihren Wert
- `exapp_settings` und `_required_exapp` sind die Form der benannten Startabweisung:
  `ToolError(message=..., hint=...)`, importiert als `from .errors import ToolError`
- `public_url(env)` liefert die öffentliche Adresse dieser Instanz ohne Schrägstrich am Ende
- `Mode = Literal["stdio", "exapp", "oauth", "http_passthrough", "http_static_bearer"]`,
  fünf Werte, und dieser Plan fügt dort **keinen** sechsten hinzu

`src/mcp_connector/oauth/metadata.py`: `RESOURCE_SUFFIX = "/mcp"`, `TOOL_SCOPE = "nextcloud"`.
`config.py` darf `metadata` **nicht** importieren (Ringgefahr, `metadata` liest `config`);
die Default-Audience wird deshalb in `chain.py` gebaut, wo beide erreichbar sind.
</interfaces>
</context>

<scope_fence>
Dieser Plan baut **nur** die Konfigurationsfläche und den Aus-Zustand. Ausdrücklich
**nicht** Teil dieses Plans:

- Keine `ChainedVerifier`-Klasse, keine Weiche über die Tokenform, kein `invalidate()`
  über zwei Schichten (Plan 22-02)
- Keine Änderung an `oauth/throttle.py`, keine neue Pfadklasse, kein Wrapper um die
  MCP-Route (Plan 22-03)
- Keine Änderung an `oauth/verifier.py`, `oauth/exchange.py`, `oauth/jwks.py`,
  `exapp/middleware.py`, `deps.py`
- Kein sechster Modus in `select_mode`, kein Eingriff in `Mode`
- Kein Konto-Mapping: der Konto-Claim wird gelesen, validiert und weitergereicht, er wird
  in dieser Phase von niemandem ausgewertet (Phase 23)
- Keine Doku unter `docs/` (die Einrichtungsdoku ist EXCH-08, Phase 24); dokumentiert wird
  in den `#:`-Kommentaren von `config.py`, im Modul-Docstring von `chain.py` und im
  `[Unreleased]`-Block von `CHANGELOG.md`
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Der Namensraum und der Schalter in config.py</name>
  <files>src/mcp_connector/config.py, tests/unit/test_config.py</files>
  <read_first>
    - `src/mcp_connector/config.py` (die ENV-Konstantenblöcke ab Zeile 40, `audit_log_enabled` als Form des ab Werk ausgeschalteten Schalters, `_TRUE_VALUES`/`_FALSE_VALUES`, `Mode`)
    - `tests/unit/test_config.py` (Testform, die Art, wie dort Umgebungen als Dicts gebaut werden, die `caplog`-Form der Warnungstests)
  </read_first>
  <behavior>
    - `config.exchange_enabled({})` ist `False`; ebenso bei leerem Wert und bei einem Wert, den niemand versteht (dann zusätzlich eine Warnung, die die Variable nennt und den Wert nicht)
    - `config.exchange_enabled({"NC_MCP_EXCHANGE_ENABLED": v})` ist `True` für jedes `v` aus `_TRUE_VALUES` und `False` für jedes `v` aus `_FALSE_VALUES`
    - `config.EXCHANGE_VARIABLES` enthält jeden Namen des Namensraums einschliesslich des Schalters, jeder Eintrag beginnt mit `NC_MCP_EXCHANGE_`, und die Sammlung ist doppelfrei
    - `config.select_mode` liefert auch mit vollständig gesetztem `NC_MCP_EXCHANGE_*` genau die Modi von heute: `exapp` bei gesetztem `APP_ID`/`APP_SECRET`, `oauth` bei `NC_MCP_AUTH_MODE=oauth`, `stdio` ohne Header
    - Die `Mode`-Literalmenge hat genau fünf Werte und enthält nichts mit "exchange"
  </behavior>
  <action>
    Ergänze `src/mcp_connector/config.py` um den Namensraum des Exchange-Pfades, in genau
    dem Stil der bestehenden Blöcke (ein erklärender Kommentarblock über den Konstanten,
    `#:`-Kommentar dort, wo ein Default begründet werden muss).

    Neue Konstanten: `ENV_EXCHANGE_ENABLED = "NC_MCP_EXCHANGE_ENABLED"`,
    `ENV_EXCHANGE_ISSUER = "NC_MCP_EXCHANGE_ISSUER"`,
    `ENV_EXCHANGE_JWKS_URI = "NC_MCP_EXCHANGE_JWKS_URI"`,
    `ENV_EXCHANGE_JWKS_ORIGIN = "NC_MCP_EXCHANGE_JWKS_ORIGIN"`,
    `ENV_EXCHANGE_AUDIENCE = "NC_MCP_EXCHANGE_AUDIENCE"`,
    `ENV_EXCHANGE_AZP = "NC_MCP_EXCHANGE_AZP"`,
    `ENV_EXCHANGE_ACCOUNT_CLAIM = "NC_MCP_EXCHANGE_ACCOUNT_CLAIM"`,
    `ENV_EXCHANGE_ALGORITHMS = "NC_MCP_EXCHANGE_ALGORITHMS"`. Dazu
    `DEFAULT_EXCHANGE_ACCOUNT_CLAIM = "sub"` mit der Begründung als `#:`-Kommentar: `sub`
    ist der einzige Claim, den Keycloak in jedem getauschten Token schreibt und den Phase 21
    bereits auf Form prüft; jeder andere Konto-Claim (etwa `preferred_username`) hängt an
    einer der vier offenen F13-Antworten und wird deshalb konfiguriert und nicht geraten.

    Dazu `EXCHANGE_VARIABLES: tuple[str, ...]` mit allen acht Namen. Der Grund gehört als
    Kommentar daneben: die Sammlung ist die Liste, gegen die `chain.load_exchange_config`
    prüft, ob jemand den Pfad konfiguriert und den Schalter vergessen hat. Eine Liste, die
    per Hand zweimal geführt wird, fällt auseinander, also steht sie hier einmal und
    nirgends sonst.

    `def exchange_enabled(env: Mapping[str, str] | None = None) -> bool` wird wortwörtlich
    nach dem Muster von `audit_log_enabled` gebaut: ab Werk aus, `_TRUE_VALUES` armen,
    `_FALSE_VALUES` entwaffnen, alles andere bleibt aus und schreibt eine Warnung, die die
    Variable und die verstandenen Schreibweisen nennt und niemals den gelesenen Wert. Der
    Docstring sagt, warum die Richtung die von `audit_log_enabled` ist und nicht die von
    `talk_send_enabled`: ein zweiter Prüfpfad, der anspringt, weil jemand sich vertippt hat,
    ist genau der Fehlschlag, gegen den CONF-01 geschrieben wurde.

    `select_mode`, `Mode`, `oauth_configured` und jede andere bestehende Funktion bleiben
    unberührt. `config.py` importiert nichts Neues.

    In `tests/unit/test_config.py` kommen die Tests aus `<behavior>` dazu, dazu der
    Aus-Zustands-Test als eigener Test mit sprechendem Namen: `select_mode` liefert mit einer
    vollständig gesetzten Exchange-Umgebung dieselben Antworten wie ohne sie, und die
    `Mode`-Literalmenge (über `typing.get_args`) hat fünf Einträge ohne "exchange".
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_config.py -q` ist grün
    - `grep -v '^\s*#' src/mcp_connector/config.py | grep -c 'ENV_EXCHANGE_'` ergibt mindestens 8
    - `python -c "from mcp_connector import config; print(len(config.EXCHANGE_VARIABLES), len(set(config.EXCHANGE_VARIABLES)))"` gibt zweimal 8 aus, und jeder Eintrag beginnt mit `NC_MCP_EXCHANGE_`
    - `python -c "from mcp_connector import config; print(config.exchange_enabled({}), config.exchange_enabled({'NC_MCP_EXCHANGE_ENABLED':'1'}), config.exchange_enabled({'NC_MCP_EXCHANGE_ENABLED':'vielleicht'}))"` gibt `False True False` aus
    - `python -c "import typing, mcp_connector.config as c; print(sorted(typing.get_args(c.Mode)))"` nennt genau die fünf Modi von heute und kein "exchange"
    - `git diff src/mcp_connector/config.py | grep -c '^-[^-]'` ergibt 0 (nichts Bestehendes wurde geändert, es kam nur etwas dazu)
    - `uv run ruff check . && uv run ruff format --check . && uv run pyright` ist still
  </acceptance_criteria>
  <done>Der Namensraum steht, der Schalter steht ab Werk aus, und der Aus-Zustand von `select_mode` ist von einem Test gehalten.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: load_exchange_config in oauth/chain.py</name>
  <files>src/mcp_connector/oauth/chain.py, tests/unit/test_oauth_exchange_chain.py</files>
  <read_first>
    - `src/mcp_connector/oauth/exchange.py` (`ExchangeSettings` und jede `ValueError`-Regel in `__post_init__`; dieser Leser muss jede davon vorher abfangen oder als `ToolError` übersetzen)
    - `src/mcp_connector/config.py` (die neuen Konstanten aus Task 1, `public_url`, die `ToolError`-Form von `exapp_settings`)
    - `src/mcp_connector/entry_oauth.py` (`load_settings`: wie dort `NC_MCP_OIDC_ALGORITHMS` kommasepariert gelesen und geprüft wird, wortwörtlich die Form für `NC_MCP_EXCHANGE_AZP` und `NC_MCP_EXCHANGE_ALGORITHMS`)
    - `src/mcp_connector/oauth/metadata.py` (`RESOURCE_SUFFIX`)
    - `tests/unit/test_oauth_exchange.py` (die Stilquelle für die Testwerte: Issuer, JWKS-URL, Audience, azp der Phase 21)
  </read_first>
  <behavior>
    - Keine einzige `NC_MCP_EXCHANGE_*`-Variable gesetzt: `load_exchange_config({})` ist `None`, und kein Default wird berechnet
    - Schalter aus, aber `NC_MCP_EXCHANGE_ISSUER` gesetzt: `ToolError`, deren Text beide Variablennamen nennt und keinen Wert
    - Schalter an, `NC_MCP_EXCHANGE_ISSUER` fehlt: `ToolError`, die `NC_MCP_EXCHANGE_ISSUER` nennt
    - Schalter an, `NC_MCP_EXCHANGE_AZP` fehlt: `ToolError`, die `NC_MCP_EXCHANGE_AZP` nennt
    - Schalter an, Issuer und azp gesetzt, sonst nichts: `ExchangeConfig` mit `jwks_uri` gleich Issuer plus `DEFAULT_JWKS_PATH`, `audience` gleich `public_url(env)` plus `RESOURCE_SUFFIX`, `account_claim` gleich `"sub"`, `algorithms` gleich `("RS256",)`
    - `NC_MCP_EXCHANGE_AZP = "a, b ,c"` wird zu `("a", "b", "c")`; ein leerer oder nur aus Kommas bestehender Wert ist eine `ToolError` und nie eine leere Allowlist
    - Jede `ValueError` aus `ExchangeSettings.__post_init__` (Issuer ohne HTTPS, Issuer mit Schrägstrich am Ende, JWKS-URL auf fremder Origin, unerlaubter Algorithmus) kommt als `ToolError` heraus, die die Variablennamen nennt, und nie als nackte `ValueError`
    - Ein leerer oder nur aus Leerzeichen bestehender `NC_MCP_EXCHANGE_ACCOUNT_CLAIM` ist eine `ToolError`; ein gesetzter Wert kommt unverändert in `ExchangeConfig.account_claim` an
  </behavior>
  <action>
    Lege `src/mcp_connector/oauth/chain.py` an. In diesem Plan enthält das Modul **nur** die
    Konfigurationshälfte; die Kette selbst kommt in Plan 22-02 in dieselbe Datei.

    Modul-Docstring: warum dieser Pfad eine eigene Datei bekommt und nicht `verifier.py`
    erweitert (der `StoreTokenVerifier` bleibt unverändert, das ist Erfolgskriterium 1 der
    Phase), warum die Konfiguration hier gelesen wird und nicht in `exchange.py` (dessen
    Zusage lautet wörtlich, dass es nichts aus der Umgebung liest, und genau das macht jede
    seiner Regeln gegen selbst gebaute Schlüssel beweisbar), und warum der Pfad ab Werk aus
    ist. Dazu die dokumentierten Defaults als Liste im Docstring: welcher Wert Pflicht ist,
    welcher einen Default hat und woher der Default kommt.

    `DEFAULT_JWKS_PATH = "/protocol/openid-connect/certs"` als Modulkonstante mit
    Begründung: das ist der Pfad, unter dem Keycloak seinen Schlüsselsatz je Realm
    veröffentlicht, und der Issuer ist die Realm-URL. Es wird ausdrücklich **keine**
    Discovery abgerufen: ein ausgehender Abruf beim Start macht eine Anbieterstörung zu
    einem Startfehler, und die Gleich-Origin-Regel von `ExchangeSettings` gilt für den
    zusammengesetzten Wert genauso wie für einen von Hand gesetzten.

    `ExchangeConfig` als `@dataclass(frozen=True, slots=True)` mit `settings: ExchangeSettings`
    und `account_claim: str`. Der Kommentar an `account_claim` sagt, dass ihn in dieser Phase
    niemand auswertet und Phase 23 (MAP-01) ihn auf ein Konto abbildet; er steht hier, weil
    CONF-01 ihn zur Konfiguration mit dokumentiertem Default macht und weil eine
    Konfiguration, die erst in der Phase entsteht, die sie braucht, dort zwischen
    Mapping-Code landet.

    `def load_exchange_config(env: Mapping[str, str] | None = None) -> ExchangeConfig | None`:

    1. Quelle ist `os.environ if env is None else env`, wie in `config.py`.
    2. Ist der Schalter aus (`config.exchange_enabled`), dann wird jede andere Variable aus
       `config.EXCHANGE_VARIABLES` auf einen nicht-leeren Wert geprüft. Findet sich einer,
       ist das eine `ToolError`, deren `message` den gefundenen Variablennamen und
       `config.ENV_EXCHANGE_ENABLED` nennt und deren `hint` sagt, dass der Pfad aus bleibt,
       bis der Schalter ihn armt, und dass ein konfigurierter, aber ausgeschalteter Pfad
       genau der stille Halbzustand ist, gegen den CONF-01 geschrieben wurde. Sonst `None`.
    3. Ist der Schalter an, werden Issuer und azp als Pflichtwerte gelesen. Fehlt einer,
       `ToolError` mit dem Namen der Variable. Kein Wert steht je in einer Meldung.
    4. Defaults: `jwks_uri` ist der gesetzte Wert oder Issuer plus `DEFAULT_JWKS_PATH`;
       `audience` ist der gesetzte Wert oder `config.public_url(source)` plus
       `RESOURCE_SUFFIX` (das ist derselbe Wert, den dieser Server für seine eigenen Tokens
       benutzt, und die gegenüber F13 dokumentierte Empfehlung aus dem Docstring von
       `exchange.py`: eine Keycloak-Client-Id je Connector-Instanz, im Zweifel die
       Resource-URL dieser Instanz, niemals ein generisches `nextcloud`);
       `account_claim` ist der gesetzte Wert oder `config.DEFAULT_EXCHANGE_ACCOUNT_CLAIM`;
       `algorithms` ist die kommaseparierte Liste oder `DEFAULT_EXCHANGE_ALGORITHMS` aus
       `exchange.py`; `jwks_origin` ist der gesetzte Wert oder `None`.
    5. `azp_allowed` und `algorithms` werden an Kommas getrennt, jeder Eintrag wird
       `strip()`t, leere Einträge fallen weg, das Ergebnis ist ein `tuple`. Ein leeres
       Ergebnis ist eine `ToolError` und nie eine leere Allowlist.
    6. Der Bau von `ExchangeSettings` steht in einem `try/except ValueError`, und die
       `ValueError` wird zu einer `ToolError`, deren `message` den Text der `ValueError`
       trägt und deren `hint` die Variablennamen des Namensraums nennt. Der Grund gehört
       als Kommentar dazu: die Regeln stehen in Phase 21 und dürfen hier nicht
       zweitgeschrieben werden, aber ein Betreiber darf keine nackte `ValueError` im
       Containerlog finden.

    Es wird in diesem Modul nichts geloggt, was aus der Umgebung kommt: kein Issuer, keine
    Audience, kein Claim-Name. Meldungen nennen Variablennamen.

    Die Testdatei `tests/unit/test_oauth_exchange_chain.py` wird neu angelegt und deckt jeden
    Punkt aus `<behavior>` mit einem eigenen Test ab. Kein Test öffnet einen Socket, und
    keiner setzt `os.environ`: jede Umgebung ist ein Dict, das hereingereicht wird.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_oauth_exchange_chain.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_oauth_exchange_chain.py -q` ist grün mit mindestens 14 Tests
    - `grep -c "def test_" tests/unit/test_oauth_exchange_chain.py` ergibt mindestens 14
    - `grep -c "ToolError" tests/unit/test_oauth_exchange_chain.py` ist grösser als 5
    - `python -c "from mcp_connector.oauth import chain; c = chain.load_exchange_config({'NC_MCP_EXCHANGE_ENABLED':'1','NC_MCP_EXCHANGE_ISSUER':'https://idp.example.org/realms/f13','NC_MCP_EXCHANGE_AZP':'f13-orchestrator','NC_MCP_PUBLIC_URL':'https://mcp.example.org'}); print(c.settings.jwks_uri, c.settings.audience, c.account_claim, c.settings.algorithms)"` gibt `https://idp.example.org/realms/f13/protocol/openid-connect/certs https://mcp.example.org/mcp sub ('RS256',)` aus
    - `python -c "from mcp_connector.oauth import chain; print(chain.load_exchange_config({}))"` gibt `None` aus
    - `python -c "from mcp_connector.oauth import chain; chain.load_exchange_config({'NC_MCP_EXCHANGE_ISSUER':'https://idp.example.org/realms/f13'})"` endet mit einer `ToolError`, deren Text beide Variablennamen enthält
    - `grep -v '^\s*#' src/mcp_connector/oauth/chain.py | grep -c 'NC_MCP_EXCHANGE'` ergibt 0 (jeder Name kommt aus `config`)
    - `grep -n "logger\." src/mcp_connector/oauth/chain.py` findet keine Zeile, die einen gelesenen Wert und nicht nur einen Variablennamen formatiert
    - `uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src scripts vulture_whitelist.py` ist still
  </acceptance_criteria>
  <done>Die Umgebung wird zu einer validierten Konfiguration, zu `None` oder zu einer benannten Abweisung, und nie zu einem halben Zustand.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Die Startabweisung in beiden Einstiegspunkten</name>
  <files>src/mcp_connector/entry_exapp.py, src/mcp_connector/entry_oauth.py, tests/unit/test_exapp_entry.py, tests/unit/test_entry_oauth.py, CHANGELOG.md</files>
  <read_first>
    - `src/mcp_connector/entry_exapp.py` (`build_exapp_app` von Zeile 70 bis 130, und `main` ab Zeile 448: der `except ToolError as other`-Zweig, der eine Bauzeit-`ToolError` schon heute in `SystemExit(2)` mit benannter Meldung verwandelt)
    - `src/mcp_connector/entry_oauth.py` (`load_settings` und `build_oauth_app`, und `main` ab Zeile 359 mit demselben Muster)
    - `tests/unit/test_exapp_entry.py` (`EXAPP_ENV`, `OAUTH_ENV`, die `TestClient`-Form, wie dort gegen `build_exapp_app` getestet wird)
    - `tests/unit/test_entry_oauth.py` (wie dort `load_settings` mit einem Umgebungsdict getestet wird)
    - `CHANGELOG.md` (der `[Unreleased]`-Block und seine Platzhalterzeile)
  </read_first>
  <behavior>
    - `build_exapp_app({**EXAPP_ENV, "NC_MCP_EXCHANGE_ENABLED": "1"})` wirft eine `ToolError`, die `NC_MCP_EXCHANGE_ISSUER` nennt
    - `build_exapp_app({**EXAPP_ENV, "NC_MCP_EXCHANGE_ISSUER": "https://idp.example.org/realms/f13"})` wirft eine `ToolError`, die `NC_MCP_EXCHANGE_ENABLED` nennt
    - `build_exapp_app(EXAPP_ENV)` ohne jede Exchange-Variable baut wie heute und schreibt keine Zeile über den Exchange-Pfad
    - Mit vollständiger Exchange-Umgebung baut `build_exapp_app` und schreibt genau eine INFO-Zeile, die sagt, dass der Pfad bewaffnet ist, ohne Issuer, Audience oder Claim-Namen zu nennen
    - `entry_oauth.load_settings` verhält sich in denselben vier Fällen genauso
  </behavior>
  <action>
    Hänge den Leser aus Task 2 an beide Einstiegspunkte, so dass eine halbe Konfiguration
    den Start abbricht statt still halb zu laufen.

    In `build_exapp_app`: unmittelbar nach dem Bau der Sicherheitseinstellungen und vor dem
    Bau von Store und Provider wird `chain.load_exchange_config(env)` gerufen. Ist das
    Ergebnis nicht `None`, geht genau eine INFO-Zeile ins Log, die sagt, dass der
    Token-Exchange-Pfad bewaffnet ist, und die keinen gelesenen Wert nennt. Der Kommentar
    daneben hält fest, dass die Verkettung selbst Plan 22-02 ist und der Aufruf hier steht,
    weil eine halbe Konfiguration einen Container nicht bedienen lassen darf: eine
    `ToolError` aus dieser Zeile fällt in den bestehenden `except ToolError as other`-Zweig
    von `main` und wird dort zur benannten Meldung mit Exitcode 2.

    In `entry_oauth.load_settings`: derselbe Aufruf, an der Stelle, an der die übrigen
    Werte validiert werden. `build_oauth_app` ruft ihn ebenfalls (die Funktion ist ohne
    `load_settings` aufrufbar und darf keine ungeprüfte Konfiguration bedienen) und schreibt
    dieselbe eine INFO-Zeile. Doppelte Validierung ist hier Absicht und bekommt einen
    Kommentar: der Leser ist eine reine Funktion seiner Umgebung, kostet nichts und deckt
    beide Aufrufwege ab.

    In `StandaloneSettings` wird **nichts** ergänzt: die Exchange-Konfiguration wird in
    Plan 22-02 dort gebraucht, wo der Verifier gebaut wird, und ein Feld, das heute niemand
    liest, fällt dem Gate zur Last.

    `CHANGELOG.md`: im `[Unreleased]`-Block ein `### Added`-Abschnitt mit einem Eintrag, der
    den Namensraum nennt, sagt, dass er ab Werk aus ist, und die vier Werte mit ihren
    Defaults auflistet (Issuer Pflicht, azp Pflicht, Audience mit der Resource-URL dieser
    Instanz als Default, Konto-Claim mit `sub` als Default). Die Platzhalterzeile "Nothing
    here is released." bleibt stehen oder weicht dem Abschnitt, je nachdem, was die Form der
    Datei verlangt; kein Release-Eintrag wird angefasst. Keine Em-Dashes, echte Umlaute.

    Tests: die vier Fälle aus `<behavior>` in `tests/unit/test_exapp_entry.py` und ihre
    Entsprechung in `tests/unit/test_entry_oauth.py`. Die INFO-Zeile wird über `caplog`
    geprüft, und zwar in beide Richtungen: sie steht da, wenn der Pfad bewaffnet ist, und
    keine Zeile des Laufs enthält den Issuer, die Audience oder den Claim-Namen.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_exapp_entry.py tests/unit/test_entry_oauth.py tests/unit/test_oauth_exchange_chain.py tests/unit/test_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_exapp_entry.py tests/unit/test_entry_oauth.py -q` ist grün
    - `uv run pytest tests/unit tests/contract -q` ist grün (volle Regression, kein Testsubset)
    - `grep -c "load_exchange_config" src/mcp_connector/entry_exapp.py` ergibt 1 und in `src/mcp_connector/entry_oauth.py` mindestens 1
    - `grep -c "NC_MCP_EXCHANGE" tests/unit/test_exapp_entry.py` ist mindestens 4
    - `grep -c "Unreleased" CHANGELOG.md` ist grösser als 0 und `grep -n "NC_MCP_EXCHANGE_ENABLED" CHANGELOG.md` findet den neuen Eintrag
    - `grep -rn "—\|–" CHANGELOG.md src/mcp_connector/oauth/chain.py` findet nichts
    - `uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run vulture src scripts vulture_whitelist.py` ist still
    - `git diff --name-only` nennt unter `src/` genau `config.py`, `oauth/chain.py`, `entry_exapp.py`, `entry_oauth.py`
  </acceptance_criteria>
  <done>Beide Einstiegspunkte bedienen eine halbe Exchange-Konfiguration nicht, und der Werkszustand ist unverändert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deploy-Umgebung zum Prozess | Jede `NC_MCP_EXCHANGE_*`-Variable entscheidet mit, welche fremden Tokens später angenommen werden |
| Admin-Overlay zum Prozess | `_resolved_env` mischt Werte aus der Nextcloud-Administration in die Umgebung; sie reisen über HTTP |
| Prozess zum Containerlog | Eine Startmeldung wird von jedem gelesen, der Containerlogs liest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Elevation of Privilege | Halbe Konfiguration | mitigate | Bewaffneter Schalter ohne Issuer oder azp bricht den Start ab; keine stille Vorgabe für die beiden Werte, die entscheiden, wer handeln darf |
| T-22-02 | Elevation of Privilege | Konfiguriert, aber nicht bewaffnet | mitigate | Jede gesetzte Variable des Namensraums ohne Schalter bricht den Start ab, statt einen Betreiber glauben zu lassen, der Pfad laufe |
| T-22-03 | Spoofing | Generische Audience | mitigate | Default ist die Resource-URL dieser Instanz, nie ein generisches `nextcloud`; ein Token für Instanz A hält damit nicht an Instanz B |
| T-22-04 | Information Disclosure | Startmeldungen | mitigate | Jede Meldung nennt Variablennamen und nie Werte; die INFO-Zeile sagt nur, dass der Pfad bewaffnet ist |
| T-22-05 | Tampering | Falscher Schlüsselsatz-Ort | mitigate | `jwks_uri` wird aus dem Issuer zusammengesetzt und von `ExchangeSettings` gegen die Gleich-Origin-Regel geprüft; keine Discovery beim Start |
| T-22-SC | Tampering | Paketinstallation | accept | Dieser Plan installiert kein Paket; der Abhängigkeitsstand stammt aus Phase 20 |
</threat_model>

<verification>
1. `uv run pytest tests/unit tests/contract -q` grün, volle Suite ohne übersprungenen Bereich.
2. `uv run ruff check . && uv run ruff format --check .` still.
3. `uv run pyright` ohne neue Meldung, `uv run vulture src scripts vulture_whitelist.py` still.
4. `python -c "import typing, mcp_connector.config as c; print(len(typing.get_args(c.Mode)))"` gibt 5.
5. `git diff --name-only` nennt unter `src/` genau die vier Dateien dieses Plans.
6. `grep -rn "check_resource_allowed" src/mcp_connector/oauth/chain.py` findet nichts.
</verification>

<success_criteria>
- Der Exchange-Pfad hat einen eigenen Namensraum mit ausdrücklichem Schalter und steht ab Werk aus.
- Audience, Konto-Claim, JWKS-URL und Algorithmen haben dokumentierte Defaults; Issuer und azp sind Pflicht.
- Eine halbe Konfiguration in beide Richtungen bricht den Start mit einer benannten Meldung ab.
- `select_mode` kennt keinen sechsten Modus, und ein Test hält diesen Aus-Zustand fest.
- Plan 22-02 kann die Kette anhängen, ohne eine Konfigurationsfrage nachzureichen.
</success_criteria>

<output>
Create `.planning/phases/22-konfiguration-kette-und-drosselung/22-01-SUMMARY.md` when done
</output>
