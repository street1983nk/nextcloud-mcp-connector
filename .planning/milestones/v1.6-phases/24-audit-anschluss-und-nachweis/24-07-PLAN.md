---
phase: 24-audit-anschluss-und-nachweis
plan: 07
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/mcp_connector/oauth/jwks.py
  - tests/unit/test_exapp_entry.py
  - tests/unit/test_oauth_jwks.py
  - .planning/BACKLOG.md
autonomous: true
requirements: [AUDIT-07]

must_haves:
  truths:
    - "Der 429-Pfad des Exchange-Zweigs ist gegen die gebaute ExApp-Anwendung gemessen und nicht nur strukturell belegt"
    - "Die Zahl der ausgehenden JWKS-Abrufe, die ein Gegenüber je Fenster bestellen kann, ist nach Phase 23 gemessen"
    - "Die gemessene Zahl steht mit ihrer Rechnung und ihrem Datum im Docstring von jwks.forget"
    - "BL-21 steht in BACKLOG.md auf RESOLVED"
  artifacts:
    - path: "tests/unit/test_exapp_entry.py"
      provides: "der gemessene 429-Lauf gegen build_exapp_app (IN-04)"
      contains: "EXCHANGE_LIMIT"
    - path: "tests/unit/test_oauth_jwks.py"
      provides: "die gemessene Abrufgrenze (IN-05)"
      contains: "call_count"
    - path: "src/mcp_connector/oauth/jwks.py"
      provides: "die gemessene Zahl mit Datum im Docstring von forget"
      contains: "forget"
  key_links:
    - from: "tests/unit/test_exapp_entry.py"
      to: "src/mcp_connector/entry_exapp.py"
      via: "TestClient gegen build_exapp_app mit bearer_call und appapi_headers"
      pattern: "build_exapp_app"
---

<objective>
Die beiden eingeordneten Befunde der Phase 22 (BL-21) werden erledigt: der 429-Pfad des Exchange-Zweigs wird gegen die gebaute ExApp-Anwendung gemessen (IN-04), und die Zahl der ausgehenden JWKS-Abrufe, die ein Gegenüber nach Phase 23 je Fenster bestellen kann, wird nachgemessen und mit ihrer Rechnung in den Docstring von `jwks.forget` geschrieben (IN-05).

Purpose: BACKLOG.md BL-21 macht beide fällig mit AUDIT-07. IN-04 ist fällig, weil die ExApp der Pfad ist, den F13 tatsächlich trifft, und die beiden bestehenden Messungen gegen `build_oauth_app` laufen. IN-05 ist fällig, weil Phase 23 die Rechnung geändert hat: vorher endete jede Exchange-Anfrage als Abweisung und wurde gezählt, seither antwortet ein gültiges Token mit 200, wird nicht gezählt und zahlt sogar eine frühere Abweisung zurück.

Output: Zwei Messungen, eine gemessene Zahl an ihrem Wirkort, und ein geschlossener Backlog-Eintrag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/BACKLOG.md
@.planning/phases/24-audit-anschluss-und-nachweis/24-RESEARCH.md
@.planning/phases/24-audit-anschluss-und-nachweis/24-PATTERNS.md

<interfaces>
Die Verträge, gegen die dieser Plan baut (gelesen am 2026-09-23):

`tests/unit/test_oauth_exchange_chain.py:1175-1220` -- der OAuth-Zwilling von IN-04:
`serve()` registriert die Schlüsselsatz-Route, `entry_oauth.build_oauth_app(standalone_env(tmp_path))`,
`EXCHANGE_LIMIT` mal `mcp_call(client, "Bearer a.b.c")` mit 401, dann 429 mit positivem
`Retry-After`, `keys.call_count == 0`, und daneben ein punktloses Token, das unverändert 401
bekommt. Dahinter, Zeilen 1204-1219, das Wortgate gegen ein Orakel im 429-Körper.

`tests/unit/test_exapp_entry.py:2117-2123` -- `bearer_call(client, token)` schickt POST `/mcp`
mit `{**MCP_HEADERS, **appapi_headers(user=""), "Authorization": f"Bearer {token}"}`.
`appapi_headers(user="")` ist der Unterschied zum OAuth-Zwilling: im ExApp-Aufbau läuft
`require_appapi` VOR der Bearer-Prüfung, und ohne diesen Header wäre der 401 der
Handshake-401 und der Test grün, ohne den Drosselpfad je erreicht zu haben.

`tests/unit/test_exapp_entry.py:2440-2447` -- `EXCHANGE_ENV` mit
`NC_MCP_EXCHANGE_ENABLED`, `NC_MCP_EXCHANGE_ISSUER`, `NC_MCP_EXCHANGE_AZP`,
`NC_MCP_EXCHANGE_ACCOUNT_CLAIM`. `tests/unit/test_exapp_entry.py:55` importiert `throttle`
bereits.

`src/mcp_connector/oauth/throttle.py:150-198` -- `CLASS_EXCHANGE = "exchange"`,
`EXCHANGE_LIMIT = 30` je Quelle, `PATH_CEILING = 200` je Klasse, `WINDOW = 300` Sekunden.
Gezählt werden **Abweisungen** (Status >= 400); ein Erfolg zahlt genau eine Abweisung zurück
(Zeilen 34-51). `CLASS_EXCHANGE_ENROLL` und `CLASS_EXCHANGE_ENROLL_START` laufen gegen
`FLOW_LIMIT = 20`; `CLASS_CONNECTIONS` gilt je Konto ohne Klassendeckel.

`src/mcp_connector/oauth/jwks.py:70-90` -- `JWKS_CACHE_SECONDS = 300`,
`JWKS_KID_COOLDOWN_SECONDS = 60`, `JWKS_FAILURE_RETRY_SECONDS = 10`.
`forget()` leert `keys` und setzt `fetched_at = float("-inf")`; die beiden
vor-authentischen Bremsen bleiben absichtlich stehen, der nächste ablaufgetriebene Abruf ist
also nicht gebremst.

`src/mcp_connector/oauth/chain.py:578-598` -- `invalidate()` ruft `forget()`, ausgelöst über
`provider.end_connection` bei jedem Widerruf.

`src/mcp_connector/audit/store.py:601-610` -- `used_bytes` ist das Muster für eine gemessene
Zahl in einem Docstring: Datum, Aufbau, Zahlen.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IN-04, der 429-Lauf gegen die gebaute ExApp-Anwendung</name>
  <files>tests/unit/test_exapp_entry.py</files>
  <read_first>
    - tests/unit/test_oauth_exchange_chain.py (Zeilen 1160-1225: der OAuth-Zwilling samt Wortgate)
    - tests/unit/test_exapp_entry.py (Zeilen 40-70 die Importe, 2100-2135 `bearer_call` und `appapi_headers`, 2430-2500 `EXCHANGE_ENV` und die beiden bestehenden Strukturtests des Wrappers)
    - src/mcp_connector/oauth/throttle.py (Zeilen 30-60 was gezählt wird, 145-200 die Klassen und Grenzen)
    - src/mcp_connector/entry_exapp.py (die Verdrahtung des Exchange-Zweigs und der Drosselung)
  </read_first>
  <behavior>
    - `EXCHANGE_LIMIT` Aufrufe mit `Bearer a.b.c` gegen die gebaute ExApp-Anwendung bekommen alle 401.
    - Der nächste Aufruf bekommt 429 mit einem positiven `Retry-After`.
    - Die Schlüsselsatz-Route wurde kein einziges Mal abgerufen (`keys.call_count == 0`): ein unlesbarer Header kostet keinen ausgehenden Abruf.
    - Ein Aufruf mit einem punktlosen Token bekommt unverändert 401: die bewusste Ausnahme der MCP-Route für eigene Tokens hält auch im ExApp-Aufbau. Das ist der eigentliche Wert dieses Tests.
    - Der 429-Körper nennt keinen Prüfschritt: dasselbe Wortgate wie im OAuth-Zwilling.
    - Ein Aufruf ohne die AppAPI-Header bekommt den Handshake-401 und erhöht den Exchange-Zähler nicht. Dieser Fall ist der Beweis, dass der Haupttest den Drosselpfad wirklich erreicht und nicht am Handshake hängen bleibt.
  </behavior>
  <action>
Baue den ExApp-Zwilling des gemessenen 429-Laufs in `tests/unit/test_exapp_entry.py`, mit `@respx.mock`, nach dem Muster aus `tests/unit/test_oauth_exchange_chain.py:1175-1220`. Die Anwendung entsteht über `entry_exapp.build_exapp_app` mit der Umgebung aus `SERVED_ENV`, `EXCHANGE_ENV` und der öffentlichen Adresse; die Aufrufe laufen über den vorhandenen Helfer `bearer_call`, der `appapi_headers(user="")` bereits mitschickt.

Der Testdocstring muss den Unterschied zum OAuth-Zwilling nennen: im ExApp-Aufbau läuft `require_appapi` vor der Bearer-Prüfung, und ohne die AppAPI-Header wäre der 401 der Handshake-401. Genau deshalb steht der Gegenfall ohne AppAPI-Header daneben, denn ein Test, der den Drosselpfad nie erreicht, ist grün aus dem falschen Grund.

Übernimm das Wortgate des OAuth-Zwillings für den 429-Körper. Wenn Plan 24-02 die Bezeichnerliste bereits aus `errors` liest, folge derselben Form; dieser Plan läuft in derselben Welle, also darf er sich nicht darauf verlassen, sondern nimmt die Wortliste, die heute dort steht.

Prüfe die `-k`-Selektoren und Funktionsnamen dieses Tests gegen die echten Namen der Datei: ein Name, der schon existiert, überschreibt den bestehenden Test lautlos.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_exapp_entry.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Der neue Test sichert genau die sechs Verhaltenszeilen zu, jede mit einer eigenen Zusicherung und einer Meldung, die sagt, was der Fall beweist.
    - `keys.call_count == 0` steht mit der Meldung dabei, dass ein unlesbarer Header keinen ausgehenden Abruf kostet.
    - `uv run pytest tests/unit/test_exapp_entry.py -q` läuft grün, und die Zahl der Tests in dieser Datei ist gewachsen (kein Name wurde überschrieben).
    - Der Gegenfall ohne AppAPI-Header ist ein eigener Testfall und kein Kommentar.
  </acceptance_criteria>
  <done>Der 429-Pfad ist auf dem Weg gemessen, den F13 tatsächlich trifft.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IN-05, die JWKS-Abrufgrenze nach Phase 23 nachmessen</name>
  <files>tests/unit/test_oauth_jwks.py</files>
  <read_first>
    - src/mcp_connector/oauth/jwks.py (Zeilen 60-100 die vier Zeitkonstanten, 150-270 `key`, die Abkühlzeit, die Fehlerkarenz, das Single-Flight, 190-215 `forget`)
    - src/mcp_connector/oauth/chain.py (Zeilen 578-598: `invalidate` ruft `forget`)
    - src/mcp_connector/oauth/provider.py (`end_connection`: wer `invalidate` auslöst)
    - src/mcp_connector/oauth/throttle.py (Zeilen 30-60 was gezählt wird, 60-75 `CLASS_CONNECTIONS`, 150-200 die Exchange-Klassen und Grenzen)
    - tests/unit/test_oauth_jwks.py (die bestehenden `respx`-Fälle und ihr Umgang mit einer injizierten Uhr)
    - .planning/phases/24-audit-anschluss-und-nachweis/24-RESEARCH.md (Abschnitt "IN-05: die Rechnung, die nachzumessen ist")
  </read_first>
  <behavior>
    - Hebel a, Widerruf plus Aufruf: ein Zyklus aus `invalidate()` und einem anschließenden gültigen Aufruf ordnet genau einen ausgehenden Abruf an, weil `forget` den Stempel auf minus unendlich setzt und der folgende Abruf damit ein ablaufgetriebener ist. Gemessen mit `keys.call_count` über n Zyklen innerhalb eines Fensters.
    - Hebel b, unbekanntes `kid`: die Abkühlzeit von `JWKS_KID_COOLDOWN_SECONDS` lässt höchstens `WINDOW / JWKS_KID_COOLDOWN_SECONDS` Abrufe je Fenster zu. Gemessen mit einer injizierten Uhr, die in Schritten der Abkühlzeit springt, statt mit einer Rechnung.
    - Hebel a ist nicht durch `CLASS_CONNECTIONS` gebremst: ein erfolgreicher Widerruf zählt keine Abweisung, also bremst die Klasse ihn nicht. Das ist gemessen und nicht aus `throttle.py` hergeleitet.
    - Der gemessene Gesamtwert je Fenster von `WINDOW` Sekunden steht als Zahl im Testnamen oder in der Fehlermeldung, damit er aus einem roten Lauf ablesbar ist.
  </behavior>
  <action>
Baue die Messung in `tests/unit/test_oauth_jwks.py` nach dem `respx`-Muster, das dort und in `tests/unit/test_oauth_exchange_chain.py` schon verwendet wird, mit `keys.call_count` als Zähler und einer injizierten Uhr für beide Hebel.

Hebel a: baue einen Zyklus aus `invalidate()` und einem gültigen Aufruf, wiederhole ihn so oft, wie ein Gegenüber es innerhalb eines Fensters von `WINDOW` Sekunden bestellen könnte, und miss den Zähler. Der Widerruf braucht in der Realität eine bewiesene Browser-Identität; das gehört als Satz in den Testdocstring, weil es den Hebel praktisch begrenzt, ohne ihn rechnerisch zu begrenzen.

Hebel b: rufe `key` mit immer neuen, unbekannten `kid`-Werten auf und lasse die injizierte Uhr in Schritten der Abkühlzeit springen. Miss, wie viele Abrufe in einem Fenster zustande kommen.

Halte die Gesamtzahl fest, nicht die Summe der beiden Teilzahlen als Rechnung: wenn die beiden Hebel sich gegenseitig beeinflussen (der Ablauf-Abruf setzt den Cache-Stempel, den der `kid`-Hebel danach vorfindet), ist die gemessene Gesamtzahl die richtige und die Summe der Einzelzahlen die falsche. Ein Testfall, der beide Hebel im selben Fenster abwechselnd zieht, misst genau das.

Der Testdocstring nennt ausdrücklich, was er nicht misst: die Zahl gilt für einen Prozess und einen Issuer; mehrere Arbeiter haben je eigene Schlüsselsatz-Instanzen.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_oauth_jwks.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Drei Testfälle: Hebel a allein, Hebel b allein, beide abwechselnd im selben Fenster; jeder mit einer gemessenen Zahl aus `keys.call_count`.
    - Der Fall zu `CLASS_CONNECTIONS` weist gemessen nach, dass ein erfolgreicher Widerruf den Zähler der Klasse nicht erhöht.
    - Die Fehlermeldung jeder Zusicherung nennt die gemessene und die erwartete Zahl.
    - Keine Zusicherung im Test rechnet die erwartete Zahl aus den Konstanten aus, ohne sie zugleich gegen den gemessenen Zähler zu halten.
  </acceptance_criteria>
  <done>Die Zahl ist gemessen, einschließlich des Falls, in dem sich die beiden Hebel gegenseitig beeinflussen.</done>
</task>

<task type="auto">
  <name>Task 3: Die gemessene Zahl an ihren Wirkort und BL-21 schließen</name>
  <files>src/mcp_connector/oauth/jwks.py, .planning/BACKLOG.md</files>
  <read_first>
    - src/mcp_connector/oauth/jwks.py (Zeilen 190-215: der heutige Docstring von `forget` mit seinen zwei Absätzen)
    - src/mcp_connector/audit/store.py (Zeilen 595-615: `used_bytes`, das Muster für eine gemessene Zahl mit Datum in einem Docstring)
    - .planning/BACKLOG.md (Zeilen 788-817: der Eintrag BL-21 mit IN-04 und IN-05 und dem Status OPEN)
    - das SUMMARY-Material aus Task 1 und Task 2 dieses Plans
  </read_first>
  <action>
Schreibe einen dritten Absatz in den Docstring von `jwks.forget`. Er trägt drei Dinge: die gemessene Zahl der ausgehenden Abrufe je Fenster von `WINDOW` Sekunden, die Rechnung, aus der sie sich zusammensetzt (die beiden Hebel und ihre Wechselwirkung), und das Messdatum. Die Form folgt `audit/store.used_bytes`: eine Zahl mit Datum in Klammern, nicht ein Satz, der eine Größenordnung andeutet. Der Absatz muss außerdem sagen, was sich durch Phase 23 geändert hat: vorher deckelten `EXCHANGE_LIMIT` und `PATH_CEILING` die Zahl der Zyklen, weil jede Exchange-Anfrage als Abweisung endete und gezählt wurde; seither antwortet ein gültiges Token mit 200, wird nicht gezählt und zahlt sogar eine frühere Abweisung zurück. Ohne diesen Satz liest der nächste Leser die alte Rechnung in die neue Zahl hinein.

Die zwei bestehenden Absätze bleiben unverändert: der erste sagt, warum die Methode existiert, der zweite, warum die beiden Zeitstempel sie überleben. Der neue Absatz widerspricht keinem von beiden und ergänzt sie.

Setze in `.planning/BACKLOG.md` den Status von BL-21 von OPEN auf RESOLVED und ergänze zwei Zeilen unter dem Status: wo IN-04 jetzt gemessen ist (Datei und Testname) und wo die Zahl aus IN-05 steht (Datei und Methode), jeweils mit dem Datum. Schreibe die Datei von Hand mit `newline="\n"`, falls du dafür Python benutzt, und setze keine Em-Dashes.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_oauth_jwks.py tests/unit/test_exapp_entry.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "2026-09" src/mcp_connector/oauth/jwks.py` ergibt mindestens 1: der Docstring trägt ein Messdatum.
    - Der neue Absatz nennt eine konkrete Zahl und die Änderung durch Phase 23.
    - `grep -c "RESOLVED" .planning/BACKLOG.md` ist um 1 gewachsen, und der Block BL-21 trägt nicht mehr `**Status:** OPEN`.
    - `.planning/BACKLOG.md` enthält keine neuen Em-Dashes (weder U+2014 noch U+2013) und keine CRLF-Zeilenenden in den geänderten Zeilen.
    - `uv run ruff format --check .` läuft grün: der neue Docstring hält die Zeilenlänge von 100.
  </acceptance_criteria>
  <done>Die Zahl steht dort, wo sie jemand sucht, und BL-21 ist geschlossen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fremder -> Exchange-Pfad der ExApp | der Pfad ist vor-authentisch und bestimmt selbst, wie viele Abweisungen gezählt werden |
| Inhaber eines gültigen Exchange-Tokens -> ausgehende JWKS-Abrufe | jeder Zyklus aus Widerruf und Aufruf ordnet einen Abruf beim Identitätsanbieter an |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-28 | Denial of Service | `oauth/throttle` im ExApp-Aufbau | mitigate | Gemessener 429-Lauf gegen `build_exapp_app` einschließlich `Retry-After` und `call_count == 0` (Task 1) |
| T-24-29 | Denial of Service (gegen den Identitätsanbieter) | `oauth/jwks.KeySet`, `forget` | mitigate | Die Grenze wird gemessen und mit ihrer Rechnung dokumentiert; wenn die gemessene Zahl gegen die Erwartung ausfällt, ist das ein Befund für das BACKLOG und kein Anlass, die Zahl zu runden (Task 2 und Task 3) |
| T-24-03 | Information Disclosure | 429-Körper | mitigate | Das Wortgate des OAuth-Zwillings wird übernommen (Task 1) |
| T-24-30 | Repudiation | ein Test, der den Drosselpfad nie erreicht | mitigate | Ein eigener Gegenfall ohne AppAPI-Header beweist, dass der Haupttest hinter `require_appapi` misst (Task 1) |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | Diese Phase installiert kein Paket (24-RESEARCH.md, "Package Legitimacy Audit") |
</threat_model>

<verification>
- `uv run pytest -q` läuft vollständig grün.
- Qualitätsgates lokal grün VOR dem Commit: `uv run ruff check .`, `uv run ruff format --check .`, `PYRIGHT_PYTHON_FORCE_VERSION=latest uv run pyright`, `uv run vulture src scripts vulture_whitelist.py`.
- Die gemessenen Zahlen stehen im SUMMARY, nicht nur im Testcode.
- Wenn die Messung aus Task 2 eine Zahl ergibt, die eine Härtung verlangt: NICHT in diesem Plan härten, sondern einen neuen BACKLOG-Eintrag anlegen und ihn im SUMMARY nennen. Diese Phase misst, sie baut keine neue Bremse.
</verification>

<success_criteria>
- Der 429-Pfad ist gegen `build_exapp_app` gemessen, einschließlich des unveränderten Alt-Pfads.
- Die JWKS-Abrufgrenze nach Phase 23 ist gemessen, einschließlich der Wechselwirkung der beiden Hebel.
- Die Zahl steht mit Rechnung und Datum im Docstring von `jwks.forget`.
- BL-21 in `.planning/BACKLOG.md` steht auf RESOLVED, mit Verweis auf beide Messorte.
</success_criteria>

<output>
Create `.planning/phases/24-audit-anschluss-und-nachweis/24-07-SUMMARY.md` when done
</output>
