# Requirements Archive: v1.6 F13 Token Exchange Identity Mapper

**Archived:** 2026-09-26
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: Milestone v1.6 "F13 Token Exchange Identity Mapper"

**Defined:** 2026-09-18
**Core Value:** Die zugänglichste und sauberste MCP-Anbindung für Nextcloud: per Klick installierbar, spec-konformes OAuth statt App-Passwort-Gebastel, und der Assistent sieht niemals mehr als der angemeldete Nutzer.

## Owner-Entscheidungen dieses Milestones

Vor der Anforderungsdefinition getroffen, aus den in `research/SUMMARY.md` gesammelten Punkten (Owner-Antworten vom 18.09.2026):

| Id | Entscheidung | Folge |
|----|--------------|-------|
| D-v1.6-01 | Credential-Weg: A und B kombiniert. Im ExApp-Modus handelt ein gemapptes Konto über AppAPI-Impersonation (Weg A); im Standalone-Betrieb wählt das getauschte Token eine vorab vom Nutzer erteilte, gespeicherte Autorisierung aus und erzeugt keine neue Vollmacht (Weg B) | Die P0-Frage aus allen vier Recherchen ist beantwortet; ~80% des Codes sind gemeinsam, der Rest verzweigt am Modus. Ein Token ohne bestehende Autorisierung wird im Standalone-Betrieb abgewiesen, nicht provisioniert |
| D-v1.6-02 | Kür: Trockenlauf-Kommando und Zwei-Konten-Negativbeweis sind im Scope; PRM/Metadata-Eintrag am Schalter und die Golden-Testfall-Wartephase sind es nicht | PRM-Ausweisung der Keycloak-Realm und die Golden-Fixture aus einem echten F13-Token wandern in die Future Requirements; getestet wird bis dahin gegen selbstgebaute Tokens plus einen lokalen Keycloak |
| D-v1.6-03 | Der Exchange-Pfad ist ab Werk aus und entsteht als Prüferkette, nicht als sechster Modus (Scope-Vorgabe des Owners vom Milestone-Start) | `select_mode` und der bestehende `StoreTokenVerifier` bleiben unangetastet; im Aus-Zustand ist die Kette verhaltensgleich zu heute |

Extern getaktet, kein Blocker für den Bau: die vier F13-Entscheidungen aus der Spec-Note (Audience-Konvention, Konto-Claim, Beispiel-Token/Realm-Export, Exchange-Ziel-Eintrag). Alles, was daran hängt, ist Konfigurationsfläche mit dokumentiertem Default.

## v1.6 Requirements

### Tokenprüfung (Exchange-Pfad)

- [x] **EXCH-01**: Die JWKS-Schicht aus `oauth/oidc.py` (Cache mit Verfallszeit, Rotation über unbekanntes kid, Algorithmen- und Schlüsseltyp-Allowlist, Gleich-Origin-Prüfung, Größenlimit, gehärteter HTTP-Client) ist in ein eigenständiges `oauth/jwks.py` herausgelöst und wird von beiden Prüfpfaden geteilt; der bestehende OIDC-Fluss verhält sich nachweislich gleich, und die zwei für den vor-authentischen Einsatz fehlenden Fähigkeiten sind ergänzt: eine Abkühlzeit gegen die Verstärkung durch unbekannte kid und ein Single-Flight gegen gleichzeitige Abrufe
- [x] **EXCH-02**: Ein eigener Prüfer nimmt ein fremdes Keycloak-JWS nur an, wenn Issuer auf der Allowlist steht, die Signatur gegen das JWKS hält (fail-closed bei unerreichbarem Schlüsselsatz), der Algorithmus und der Schlüsseltyp erlaubt sind, die Standard-Claims (iss, exp, iat, aud als Pflicht per require-Option, nbf wenn vorhanden) mit Clock-Skew-Toleranz gelten und der typ-Claim ein Access-Token ausweist; ein ID-Token wird abgewiesen
- [x] **EXCH-03**: Die Audience wird exakt verglichen (strict_aud), ausdrücklich nicht über `check_resource_allowed`; die handelnde Partei wird über eine azp-Allowlist geprüft, weil Keycloaks Standard Token Exchange keine act-Delegationssemantik trägt
- [x] **EXCH-04**: Die Auswahl zwischen eigenem Token und Exchange-Token fällt über die Tokenform (punktloser Store-Wert gegen kompaktes JWS mit zwei Punkten) vor jeder Prüfung; der `StoreTokenVerifier` bleibt unverändert, und ein heute gültiges Token erreicht den neuen Code in keinem Fall
- [x] **EXCH-05**: Exchange-Prüfungen sind vor-authentisch drosselbar: die bewusste Ausnahme der MCP-Route in `throttle.py` gilt nicht für den neuen Pfad (dort stehen RSA-Prüfung und potenziell ein JWKS-Abruf vor der Authentisierung), und der Docstring der Ausnahme zieht in derselben Änderung mit

### Konto-Mapping

- [x] **MAP-01**: Das Claim-Mapping auf ein Nextcloud-Konto ist konfigurierbar mit mindestens einem sub-basierten und einem LDAP-tauglichen Profil, und das Ergebnis ist der kanonische Principal (nicht der Anmeldename): Pausenschalter, Audit-Kettenname und Sweep wirken für ein gemapptes Konto nachweislich genauso wie für ein angemeldetes, von einem Test gehalten
- [x] **MAP-02**: Ein Token, dessen Claim auf kein existierendes Nextcloud-Konto zeigt, wird abgewiesen; es gibt keine stille Kontoanlage, und die Kontoexistenz-Prüfung entscheidet fail-closed (die umgekehrte Asymmetrie zum bewusst nachsichtigen Audit-Sweep)

### Credential-Wege (D-v1.6-01)

- [x] **CRED-01**: Im ExApp-Modus handelt ein per Exchange gemapptes Konto über AppAPI-Impersonation, ohne dass je Nutzer etwas provisioniert werden muss; die Rechtegrenze bleibt in Nextcloud geprüft
- [x] **CRED-02**: Im Standalone-Betrieb wählt das getauschte Token eine bestehende, vom Nutzer vorab im Browser erteilte und gespeicherte Autorisierung aus; es erzeugt keine neue Vollmacht, die Zuordnung ist für den Nutzer sichtbar und einzeln widerrufbar, und ein Token ohne solche Autorisierung wird mit einem unterscheidungsfreien Fehler abgewiesen

### Konfiguration und Schalter

- [x] **CONF-01**: Der Exchange-Pfad hat einen eigenen Konfigurationsnamensraum (NC_MCP_EXCHANGE_*), steht ab Werk aus und ist ohne die vier F13-Antworten sinnvoll konfigurierbar (Audience, Konto-Claim, Issuer, azp als Werte mit dokumentierten Defaults); im Aus-Zustand ist das Verhalten byte-gleich zu heute, von einem Test gehalten

### Audit

- [x] **AUDIT-07**: Ein über Exchange handelnder Werkzeugaufruf steht in der bestehenden hash-verketteten Audit-Kette samt handelnder Partei (azp), unter den bestehenden Inhaltsverboten; abgewiesene Exchange-Versuche sind für den Betreiber sichtbar (heute stünden sie in keiner Zeile), ohne Token-Inhalte zu protokollieren

### Betrieb und Nachweis (D-v1.6-02)

- [x] **EXCH-06**: Ein Trockenlauf-Kommando prüft ein vorgelegtes Token gegen die aktive Konfiguration und benennt das Ergebnis je Prüfschritt, ohne dass das Token dabei irgendetwas darf; damit lässt sich die F13-Einrichtung ohne Live-Zugriff verproben
- [x] **EXCH-07**: Ein Zwei-Konten-Negativbeweis liegt als Messdatei neben den bestehenden Client-Nachweisen: zwei gemappte Konten, und keines sieht die Dateien des anderen, gemessen und nicht argumentiert
- [x] **EXCH-08**: Eine Einrichtungsdoku (docs/) beschreibt den Pfad von der Keycloak-Seite bis zum ersten Werkzeugaufruf, einschließlich der empfohlenen Audience-Konvention, des occ-oauth2:add-client-Playbooks und einer ehrlichen Grenzbeschreibung (was der Pfad nicht leistet, was von F13s Antworten abhängt)

### Abhängigkeiten

- [x] **DEP-01**: PyJWT steht auf >=2.14,<3 (Lock 2.14.0), cryptography zieht im selben Lock-Schritt mit, und `docs/dependency-audit.md` trägt den Nachtrag zur Sicherheitsfreigabe vom 11.09.2026; alle sechs Versionsstellen-Gates bleiben grün

## Future Requirements

Anerkannt, aber nicht in diesem Milestone.

### Extern getaktet (F13)

- **EXCH-F01**: Golden-Testfall aus einem echten F13-Beispiel-Token und Realm-Export, als eingefrorene Fixture offen im Repo (Zusage aus der Spec-Note, Abschnitt 7); wartet auf F13-Entscheidung 3
- **EXCH-F02**: Die vier F13-Konfigurationswerte (Audience-Konvention, Konto-Claim, azp, Exchange-Ziel-Eintrag) als verprobte, dokumentierte Werte statt Defaults; wartet auf das zweite Treffen
- **CLIENT-02**: F13 verbindet sich live, Messdatei plus belegte Doku-Sektion (unverändert deferred aus v1.1, jetzt mit realem Pfad)

### Bewusst nicht gewählt (D-v1.6-02)

- **EXCH-F03**: RFC-9728-PRM-Eintrag nennt die Keycloak-Realm als zweiten Authorization Server, gekoppelt an den Schalter; Owner hat die Kür nicht gewählt, die Spec-Frage (MUST-Formulierungen der MCP-Spec bei zwei Issuern) bleibt in der Doku als Grenzbeschreibung benannt

### Aus früheren Milestones unverändert

- **EXAPP-12**: Auslieferung als Store-Release nach dem Runbook, Zeitpunkt ist ein eigener Owner-Entscheid
- **CLIENT-01/03**: MUCGPT/BaerGPT live, extern getaktet

## Out of Scope

- Ein sechster Modus in `select_mode` oder Änderungen am `StoreTokenVerifier` (D-v1.6-03)
- Introspection oder andere Netzaufrufe im heißen Pfad außer dem zwischengespeicherten Schlüsselsatz
- Stille Kontoanlage, E-Mail oder Anzeigename als Mapping-Schlüssel, Rollenauswertung im Connector
- Token-Passthrough oder eigenes Horten fremder Tokens
- Subject-Impersonation und Delegations-Flows, die Keycloak selbst noch nicht ausliefert

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| EXCH-01 | Phase 20 | Complete |
| DEP-01 | Phase 20 | Complete |
| EXCH-02 | Phase 21 | Complete |
| EXCH-03 | Phase 21 | Complete |
| CONF-01 | Phase 22 | Complete |
| EXCH-04 | Phase 22 | Complete |
| EXCH-05 | Phase 22 | Complete |
| MAP-01 | Phase 23 | Complete |
| MAP-02 | Phase 23 | Complete |
| CRED-01 | Phase 23 | Complete |
| CRED-02 | Phase 23 | Complete |
| AUDIT-07 | Phase 24 | Complete |
| EXCH-06 | Phase 24 | Complete |
| EXCH-07 | Phase 24 | Complete |
| EXCH-08 | Phase 24 | Complete |

Abdeckung: 15 von 15 v1.6-Requirements genau einer Phase zugeordnet, keine Waise, keine Doppelung. Die Future Requirements (EXCH-F01..F03, CLIENT-01..03, EXAPP-12) sind bewusst ohne Phase.
